# ALFWorld V1.8 Oracle Pose And Typed Feedback Delivery Plan

## Goal

Implement and deliver the user-approved V1.8 Oracle-pose/typed-feedback design on `visualagentloop`, preserve Gate A and Gate B per-instance outcomes without requiring a perfect pass, complete regressions and documentation, and make local commits without pushing. Launch the fixed ten-Episode real-API regression from the frozen selection rule and expose every failed row without substitution.

## Non-Negotiable Gates

- The V1.8 architecture review, exact implementation plan and one-time plan review are complete. The original perfect-Gate-A prerequisite was explicitly superseded by the user's 2026-07-17 execution override; external failures remain visible but do not block product edits or the local commit.
- Root cause and boundary evidence precede fixes; failing tests precede implementation.
- Completion requires unit tests, interface implementation audit, THOR return-code checks, and per-target black-box terminal-state assertions.
- Review budget is fixed: one implementation-plan review and one final code review. Findings are dispositioned by the main agent; no second review round is allowed.
- The first real Gate A invalidated the reviewed pose-source architecture. The user approved a major reset-time scan replacement on 2026-07-15; one scoped independent delta-design review is now mandatory before implementation because the prior review could not cover this new external data flow. It is not an automatic second review loop.
- Gate A/B failures must return to the approved design boundary. Candidate, legacy, expert-trajectory, or other fallback modes are forbidden.

## Phases

| Phase | Status | Exit Criterion |
|---|---|---|
| 1. Recover context | complete | Specs, issue record, failed episode, code paths, dirty worktree, and prior evidence are mapped. |
| 2. Design alternatives and first review | complete | 3-4 candidates and recommendation are reviewed independently; every comment is dispositioned. |
| 3. Real-environment verification | complete | All MVP linchpins and per-instance pose budgets have authoritative evidence. |
| 4. Revised design review | complete | Review findings were dispositioned; the user approved implementation without another review round. |
| 5. TDD implementation | complete | Navigation, put, feedback, outcome, taskset and coverage RED tests now pass. |
| 6. Verification | complete | Unit, interface-audit, six-Shelf characterization, and Shelf 3/4/6 product black-box gates pass. |
| 7. Documentation and final regression | complete | Architecture, guide, README, CHANGELOG, pitfalls, handoff state and final test run agree. |
| 8. Recover V1.8 context | complete | User decisions, real-run root causes, subagent evidence, canonical remote commit and clean worktree are reconciled. |
| 9. Independent V1.8 design review | complete | Reviewer returned FIX; the separate code-boundary audit confirmed the concrete bypasses without endorsing runtime symbols. |
| 10. Review disposition and self-audit | complete | All findings were accepted and edited; contract wording, code fences, whitespace and secret scans pass. |
| 11. Design documentation commit | complete | Spec, CHANGELOG and live handoff docs agree; secret scan and `git diff --check` pass; design-only commit is pushed. |
| 12. User design approval | complete | User explicitly approved implementation from commit `22cb122`; stale waiting-for-approval text is synchronized. |
| 13. Exact implementation plan | complete | Code/test/doc boundaries, RED commands, Gate A/B matrix, exact trial appendices and deletion guards pass main-agent self-review. |
| 14. One-time implementation-plan review | complete | The only review returned FIX; all ten findings were accepted into plan/spec and recorded, with no second review. |
| 15a. Gate A design recovery | complete | The bounded scan/snapshot architecture, failure rules and verification gates are written, delta-audited, self-audited, locally committed and explicitly advanced to execution by the user. |
| 15. Gate A runtime feasibility | complete_with_failures | Runs through 015 are preserved. Run 015 completed 20 workers with 19 PASS and one supplemental Slice reset-settling failure; the user removed perfect Gate A as a product-edit prerequisite on 2026-07-17. |
| 15b. Containment authorization delta | superseded | The written spec was self-audited, committed and synchronized, then superseded before implementation by the newly learned visible-only navigation constraint. |
| 15c. Visible-only navigation delta | complete | The all-target current-visibility rule, post-visibility snapshot role and paired black-box gates are self-audited, synchronized and explicitly approved by the user. |
| 15d. Visible-only implementation plan | complete | The stale plan is replaced by a 1,014-line exact Gate/TDD/code/doc plan and passes main-agent coverage/type/placeholder/path/command/fence/credential/whitespace review. |
| 15e. One-time visible-only plan review | complete | The sole read-only review of candidate `9af25b8314b696d43220ec8833d223b87410ea5dd079ed43da991bc4e0e66938` returned FIX; all five findings were accepted and the final plan is locked at `f9f8f998a779754c538ff9a2e931ec1f463c58e1297d1a84a4489a4928c977ab`. No second plan review is permitted. |
| 15f. Simulation-time transaction delta | complete | The user explicitly approved implementation from the self-audited spec committed as `61b76dc` and byte-identical on hkust4 at SHA-256 `92ef48a68a30f021aa81cadf5177e07ecf9d12ecda924744a50a4c6f612a9f49`. |
| 15g. Controlled-time implementation-plan delta | complete | The existing reviewed plan is updated in place for matrix/probe/controller/verifier, failure recovery, effect control and run-008 at SHA-256 `ed91439f0ae0f6453084c37f95032600467e8a4e5d225f1c60c99877e916bd71`; 1,207 lines, 70 fences and all self-audits pass. No second plan review is permitted. |
| 16. TDD product implementation | complete | Trial/reset/snapshot/model-view/exact execution/feedback/provider/runner lifecycle paths and focused regressions are implemented. V1.7 compatibility bodies remain physically present but are unreachable from the formal V1.8 call graph. |
| 17. Gate B and full verification | complete_with_failures | `run-001` exposed and fixed a keyword-only runner call; `run-002` reached THOR and terminated honestly at reset recovery. After direct review added exact runtime-scene validation, final production-affecting rerun `run-003` reached the same honest terminal. The complete matrix remains unavailable without `exact-cases-v3.json`. |
| 18. Documentation and Chinese report | complete | Architecture, guide, README, CHANGELOG, live handoff and the Chinese evidence report agree with the failed/incomplete external evidence and final internal counts. |
| 19. One-time code-review round | complete_direct | The active no-subagent constraint replaced the planned independent reviewer with one main-agent complete-diff review. It found and fixed the runtime-scene identity omission; no further blocking defect was found. |
| 20. Local commit, runtime sync and real-API launch | complete_with_failures | The ignored Mimo profile passes a real `LLMClient` probe. The committed ten-entry manifest pins six exact trials plus four pre-Gate `candidate-1` replacements. The ten-Episode process produced all ten rows, each failed before Provider construction at `scan_pose_mismatch -> scan_time_scale_restore_rejected`; no push is authorized. |

## Errors Encountered

| Error | Attempt | Resolution |
|---|---:|---|
| Remote planning files were absent | 1 | Initialize them before further investigation. |
| `jq` is not installed on `hkust4` | 1 | Use Python's standard `json` parser for JSONL evidence; do not install or change project dependencies. |
| Multiline `python3 -c` arrived over SSH with literal `\\n` characters | 1 | Switched to newline-free one-line parsers; do not repeat the multiline invocation form. |
| Broad `/tmp` `find` predicate emitted unrelated permission noise | 1 | Stop scanning `/tmp`; use the already identified Xvfb auth paths directly. |
| Project environment `pip check` reports `textworld 1.7.0 is not supported on this platform` | 1 | Do not change dependencies; first determine whether current ALFWorld runtime/experiments execute and record the check as an environment caveat. |
| Assumed the target task directory directly contained `traj_data.json` | 1 | It contains three trial subdirectories; identify the exact trial from scene/object-pose evidence before loading one. |
| Invalid-type `gridSize` was silently ignored by `GetReachablePositions` | 1 | Record this as the real contract; do not use it as a failure probe or assume request validation. Exceptions/missing events remain terminal-uncertain paths. |
| First open probe selected hidden `Blinds` and returned failure with empty detail | 1 | Re-ran from an independent reset with a visible closed Drawer; return code and `isOpen` transition passed. |
| Toggle probes against hidden CellPhone, DeskLamp, and LightSwitch returned `object not found` | 1 | Keep toggle terminal-state contract UNVERIFIED; a later focused probe must first satisfy an exact-target navigation/observation gate. |
| Local staging `types.py` overwrote newer taskset fields during remote sync | 1 | Restored the taskset patch from its owner, preserved `backend_action_count`, re-synced remote back to staging, and reran outcome/runner/CLI plus the full benchmark suite. |
| Existing V1.7 local staging was behind remote HEAD and carried the whole prior delivery as uncommitted changes | 1 | Treat `hkust4` commit `0fdfeaa` as canonical and create a fresh isolated V1.8 clone; never upload the stale tree. |
| Initial SSH-backed `git clone` was denied by the local network sandbox | 1 | Re-ran the same scoped clone with explicit approval; the isolated clone completed. |
| Combined remote doc check embedded Markdown backticks in nested shell quoting and ended with an unexpected EOF | 1 | Do not repeat that command form; the local balanced-fence check already passed and the remote spec hash is identical. Run the remote secret scan separately without backticks. |
| Initial file-count command assumed `src/homemaster/agent/tool_dispatcher.py` | 1 | Located the real implementation at `src/homemaster/tools/dispatcher.py`; use repository symbol search before naming plan paths. |
| One wide multi-file documentation patch failed on a mid-spec context match | 1 | Confirmed no partial write, split the change into narrow file-local patches, and applied each against the observed lines. |
| Parallel existence checks opened 20 SSH connections and six were closed by the remote host | 1 | Treated connection closure as transport failure rather than missing-file evidence; reran the six affected paths sequentially and all passed. Future remote batch checks use one process or bounded concurrency. |
| Initial `scp` synchronization was blocked by the local network sandbox | 1 | Re-ran through the required execution approval path; local/remote SHA-256 values then matched for all five Markdown files. |
| Main-thread fake-summary audit first assumed a nested `workers[*].process.return_code` shape | 1 | Printed the real worker keys, switched to flattened `process_return_code`/`observed_status`, and recomputed all 166 workers; future Gate A parsing is schema-first. |
| Minimal Oracle smoke produced no JSON and Player.log ended with `ArgumentNullException(name)` | 1 | Phase markers proved reset/move succeeded; the actual failure was JSON serialization of an ndarray bbox. A one-variable bbox normalization run exited 0. The Player.log exception occurs independently during ai2thor 2.1.0 teardown. |
| Verifier owner reported “Ruff PASS” after running lint but not the separate format gate | 1 | Main-thread `ruff format --check` found one formatting delta; mechanically formatted the verifier and reran compile, lint, format, PASS fixture, and four mutation/missing-artifact gates successfully. |
| Initial Gate A helper composition would execute cases immediately after discovery | 1 | Main audit found no independent verification point between manifest freeze and actions. Split the controller/verifier into explicit discovery and run phases; run must consume the already verified manifest/hash and may not rediscover. |
| Initial discovery parsed but did not enforce the matrix's five movable-location profiles or persist a complete Oracle-map inventory | 1 | Add canonical Oracle entry evidence, exact map-ID-to-case coverage, deterministic surface/open/closed/floor/inventory classification, and mutation tests that remove each category/instance independently. |
| Controller split fixture passed its own checks but failed the independent discovery verifier | 1 | The frozen fake cases omitted `expected.navigation_context_expected`; require all shared action-count/context fields in controller case validation, regenerate the split fixture, and require the latest verifier discover/run commands to consume the same artifacts successfully. |
| Split v6 case semantics passed but the independent run verifier rejected helper identity | 1 | Controller fixed a worker-summary trial hash field after v6 was generated, so recorded controller bytes no longer matched. Invalidate v6 completely and regenerate discovery/run from frozen final helper bytes; never rewrite an identity hash around old evidence. |
| First real Gate A discovery stopped on `episode-0001-candidate-1` | 1 | Preserve `discovery-run-001` unchanged and return to root-cause investigation. The raw reset reports `FloorPlan219_physics`, no eligible movable profile, and no positive `use` case; product implementation remains frozen. |
| Assumed the matrix-relative V1.8 trial path lived under the HomeMaster repository | 1 | Direct open returned `FileNotFoundError`; the recorded worker command identifies `/data1/haodong2/weilin/red_bird/alfworld` as the authoritative root. Use that exact root for further reads rather than scanning unrelated filesystem roots. |
| Gate A compared ALFWorld's base floor-plan identity directly with AI2-THOR's Unity asset identity | 1 | Source and 541 real event artifacts prove ai2thor 2.1.0 maps `FloorPlanN` to `FloorPlanN_physics`. Replace the naked equality only after a RED contract test; keep unseen `FloorPlan10_physics` runtime availability `UNVERIFIED` until its trial runs. |
| A reset-boundary search named a nonexistent `src/homemaster/benchmarking/alfworld/runtime.py` | 1 | `rg` returned exit 2 but still found the real boundaries in `runner.py` and `env_adapter.py`. Drop the nonexistent path and use repository file discovery before the next targeted read. |
| Initial local `rg` placed `--glob` after `--`, so glob options were treated as paths | 1 | Corrected the option order and reran successfully; keep `--` immediately before the pattern only after all options. |
| A broad remote evidence `rg` produced about 3.3M tokens before truncation | 1 | Stop repository-wide raw-event searches. Use exact evidence paths from structured trace references and project artifacts, then parse only named lines/fields. |
| Read-only `scan_spec_delta_map` agent stream disconnected before completion | 1 | No file was modified. Main thread completed the section/symbol map; after the draft freezes, retry the same agent with a narrower final-bytes contradiction audit rather than repeating the broad mapping task. |
| Default-mode `request_user_input` was accidentally invoked during context recovery and rejected as unavailable | 4 | No state changed and no user input was needed. Stop invoking it; continue with read-only inspection and reasonable in-scope assumptions. |
| A nonexistent deferred exec cell was polled during the final-draft audit | 1 | The call failed before any command or state change. Use ordinary completed commands for the remaining audit. |
| A wide typed-reset/goal-advance patch missed one changed context line | 1 | `apply_patch` rejected the whole patch with no partial write. Split it into schema, invariant and behavior patches against the current bytes. |
| A second nonexistent deferred exec cell was accidentally polled before final self-audit | 1 | It failed before any command/state change. Complete all remaining checks with ordinary non-deferred commands. |
| Direct `git push origin visualagentloop` was rejected because origin is a non-bare worktree with that branch checked out | 1 | Do not change `receive.denyCurrentBranch` or force-push. Verify the remote dirty files are this task's earlier synchronized draft, sync only the six owned docs, commit in the remote worktree, then reconcile local/origin SHAs. |
| Main-thread matrix-v1/v2 bijection audit assumed nonexistent `id` and `public_semantic_vocabulary_sha256` fields | 2 | Printed the actual schema, switched to `trial_id` and `public_vocabulary_sha256`, then proved all 20 legacy trial rows byte-equivalent on locked fields and the 25-item vocabulary hash correct. |
| V2 mutation self-tests were GREEN while the real case worker and run verifier still used the old map/count-only paths | 1 | Added AST-backed entrypoint audits, a probe snapshot-lookup self-test, fresh per-case reset/scan/restore wiring, matrix/hash propagation, and independent per-case raw-artifact derivation. A valid synthetic case setup now has zero verifier failures while a tampered snapshot is rejected. |
| First containment-state live-doc patch assumed a summary sentence existed verbatim in `findings.md` | 1 | `apply_patch` rejected the whole multi-file patch with no partial write. Read the real file tails and use narrow, file-specific contexts. |
| Final spec scan embedded Markdown backticks inside a JavaScript template literal | 1 | The orchestration script failed to parse before any command ran. Rebuild every pattern as an ordinary quoted string with shell single quotes; do not interpolate Markdown backticks. |
| Gate A README fence audit repeated the JavaScript-template/backtick parse error | 1 | No shell command or write ran. Replaced the template literal with ordinary strings and `chr(96) * 3`; the rerun proved 14 balanced fences and no stale v2 command/path hits. |
| Ruff passed in `/tmp` but failed under the formal repository configuration; combined SSH still exited 0 | 1 | Root cause was missing explicit `--config` outside the repo plus no fail-fast shell. Fixed two `B023` closures and one `E501`, formatted all helpers with the real `pyproject.toml`, reran local compile/self-tests, and added permanent pitfalls/CLAUDE rules. |
| Gate B `run-001` failed before Adapter construction with a keyword-only signature mismatch | 1 | Preserve `run-001`, pass `first_trial_path` explicitly by keyword, add a strict-signature runner regression, and use fresh `run-002`. |
| Gate B `run-002` stopped during reset recovery | 1 | Preserve the `scan_pose_mismatch -> scan_time_scale_restore_rejected` terminal, five setup actions and zero Provider requests as non-blocking evidence; do not substitute a different outcome-selected trial. |
| Real `discovery-run-004` stopped after one externally successful trial because controller required an oracle for the intentionally missing ordinal sentinel | 1 | Preserve run-004 unchanged. Worker exit/status/setup/cleanup are PASS with 27 setup and zero tested/model actions; only controller manifest validation failed. Add a RED actual-entrypoint guard, special-case and independently derive `grounding_ordinal_missing` bindings, archive run-004 helper bytes, then use fresh `discovery-run-005`. |
| Real `discovery-run-005` stopped at trial 2 because probe appended a parent-anchor error before applying a valid direct snapshot override | 1 | Preserve run-005 unchanged. Added a RED direct-anchor behavior case, moved the override before the final missing-anchor check, and replayed the full real trial into 31 cases with empty issues. Archive run-005 bytes and use fresh `discovery-run-006`; a third composition failure triggers a broader architecture audit before another patch. |
| First fixed-probe offline import failed inside `dataclass`, then full replay lacked `traj_data_file_sha256` | 2 | Replay harness omissions only: register the dynamic module in `sys.modules`, then reproduce the worker's documented runtime-added trial hash. The unchanged run-005 artifacts subsequently replayed to 31 cases with empty issues. |
| A read-only remote `python -c` JSON query lost its command quoting through `ssh` | 1 | No evidence was read or changed. Quote the complete remote command as one shell argument and use double quotes only inside Python; do not repeat the split command form. |
| A local read-only matrix summary embedded escaped mapping keys inside an f-string | 1 | Python failed at parse time and read no file. Use `.format()` or a small parser file for nested structured output; do not repeat the nested f-string form. |
| A verifier condition extension retained the old argument-separating comma before added boolean clauses | 1 | The malformed bytes were caught by immediate source inspection before execution. Remove only the stray comma and run `py_compile` before any further verifier edit. |
| Gate A v3 verifier rejected a valid five-field fixture pose as a full `TeleportFull` request | 1 | Reproduced the exact schema failure, traced it to `_validate_v3_visibility_fixture()`, and changed only the two fixture-pose calls from `validate_requested_pose_schema()` to `validate_actual_pose_schema()`; compile and all 16 verifier v3 checks now pass locally. |
| Latest Gate A v3 helper bytes passed Ruff lint but all three failed `ruff format --check` after same-batch/report edits | 1 | Ran Ruff 0.15.20 formatter only on isolated `hkust4:/tmp` copies, reran lint and format-check successfully, and synchronized the formatted helper bytes back locally by exact SHA-256. |
| New controller dead-authority guard failed on three legacy discovery validators | 1 | Expected RED: the guard named `validate_initial_state_raw_event`, `validate_movable_profiles`, and `validate_oracle_entries`, matching the proven unreachable post-v3 branch. Remove that branch and only its orphaned validators, then rerun the same guard. |
| Controller accepted a v3 discovery result with the wrong `matrix_sha256` | 1 | An isolated stub test proved the active dispatch ignored the persisted hash because only the deleted legacy branch compared it. Added a pre-artifact exact hash check at the v3 dispatch boundary; the same mutation is now rejected. |
| Verifier lacked the planned complete `CaseBundle` data-flow gate | 1 | Added five named RED checks for valid bundle plus snapshot/model-view/fixture-event/action-count tampering; self-test exits 1 solely because those five are explicitly not implemented. Implement each against fresh temporary artifacts and require category-specific rejection. |
| Final verifier CaseBundle helper failed Ruff format-check after implementation | 1 | Ruff lint passed; formatted only the isolated remote verifier copy, reran lint and format-check to PASS, and synchronized the formatted bytes back locally at SHA-256 `24d34672...`. |
| First post-Open offline replay stopped with `KeyError: 'restored_event'` | 1 | Audit-harness omission only: `_derive_discovery_cases()` composes `_grounding_cases()`, whose missing-ordinal fixture consumes the worker transaction's in-memory `restored_event` and `restored_capture`. Reconstruct both from immutable raw event/frame artifacts before judging any helper validator. |
| A read-only restored-state query used nonexistent bare `python` on `hkust4` | 1 | No state changed. Use the locked runtime interpreter `/data0/yuqiao/envs/hm_alfworld/bin/python` for every Gate command and query. |
| First repaired replay required every worker to contain a post-Open case | 1 | The immutable run-004 candidate-1 legitimately has no eligible closed child. A scan then proved all seven preserved workers have zero post-Open coverage, so report that absence explicitly and reserve the nonzero/both-outcome hard gate for fresh run-006 rather than manufacturing historical coverage. |
| A read-only movable-profile summary serialized tuple keys as a JSON object | 1 | No state changed. Emit profile/count tuples as a list of records; the corrected query proved run-005 candidate-2 contains only eligible surface and inventory profiles. |
| Project-config Ruff rejected one 101-character probe self-test line | 1 | Fail-fast stopped before later gates. Mechanically wrapped only the AST source assignment, then restart compile/lint/format and every self-test from the beginning. |
| Project Ruff format-check required all three post-Open helper copies to be reformatted | 1 | Lint passed but fail-fast stopped before self-tests. Format only the isolated remote copies with the explicit repository config, rerun every gate, sync exact formatted bytes back locally, and rerun again. |
| Isolated formatted controller self-test lacked adjacent `matrix-v3.json` | 1 | Audit-directory omission only; compile/lint/format and probe self-test had passed before fail-fast stopped. Copy immutable matrix-v2/v3 bytes beside the helpers and restart the entire gate sequence. |
| Multi-source live-doc `scp` flattened `docs/pitfalls.md` to repository-root `pitfalls.md` | 1 | The extra file was created by this session and no existing file was replaced. Delete only that mistaken root file, sync the document to `docs/pitfalls.md`, then re-audit remote status and hashes. |
| Real `discovery-run-006` stopped at trial 4 with no unique-parent post-Open row | 1 | Preserve run-006 unchanged. All four workers cleaned up; setup total is 106 and tested/model remain zero. The failing trial has two eligible closed-container children but `_post_open_cases()` produced no unique-parent sequence. This is the third real composition failure: perform a broader candidate/snapshot/oracle architecture audit before any repair or new run. |
| Production-shape post-Open child binding tests failed across all three helpers | 1 | Expected RED with compile PASS: probe rejected a closed child whose visibility oracle correctly had no execution anchor; controller named the forbidden authority; verifier's sole new wiring check failed while all prior checks passed. Remove only that impossible cross-layer dependency and derive the binding from raw reset containment plus parent snapshot. |
| Probe GREEN rerun reached an incomplete synthetic container fixture | 1 | The intended anchor RED was gone, then `_post_open_cases()` required existing `source`/frame-binding fields. Add only those production fixture fields to the new test data; controller/verifier were already GREEN. |
| First multi-file synthetic-fixture patch had a malformed hunk boundary | 1 | `apply_patch` rejected the entire patch before writing. Reissued the same narrow fixture/live-doc change with valid file boundaries. |
| Run-006 repair probe needed project formatter output | 1 | Compile/lint passed; fail-fast stopped before remote self-tests/replay. Format only the isolated probe, restart all gates, then synchronize the exact formatted byte back locally. |
| First run-006 repair replay still lacked post-Open rows after child binding decoupling | 1 | The repair passed 26/42/36 internal checks but immutable trial 4 replay retained the original issue. Root cause moved one layer upstream: production visibility oracles excluded all target-independent public containers because coverage used only trial requested types. Audit the bounded scan before changing coverage. |
| Local RED-baseline orchestration referenced unavailable JavaScript `process.env` | 1 | The tool script failed before launching any project command or changing any file. Re-ran with explicit `python3` and recorded the real compile/self-test exits. |
| Two live-document patches used a shared tail line from the wrong file | 2 | Both `apply_patch` calls failed atomically before writing. Switched to one-file patches anchored to each file's numbered final line. |
| First isolated remote gate command omitted the outer `ssh hkust4` | 1 | Candidate files had copied successfully, but the local shell exited 127 before any remote check. Re-ran the complete fail-fast sequence through SSH in the same isolated directory. |
| First local archive comparison left `$b` undefined in the shell | 1 | Archive creation had succeeded, but comparison exited 2 before reading files. Defined paths inside the shell and proved all five archive/formal pairs byte-identical. |
| First formal README audit assumed at least six trailing-slash run-007 hits | 1 | Printed the real string counts: exactly four discovery and three case hits. Changed the audit to exact observed format counts and restarted the entire preflight. |
| Formal preflight rerun embedded shell `${...}` in a JavaScript template | 1 | The orchestration script failed to parse before issuing SSH. Removed the unnecessary expansion and reran from the first gate. |
| Fresh `discovery-run-007` stopped at trial 5 on temperature-only world drift | 1 | Preserve run-007 unchanged. Step 3 Teleport succeeded and matched its pose, but two Eggs and one Lettuce changed raw `ObjectTemperature` from `Cold` to `RoomTemp`; no transform, containment, runtime set or goal field changed. Treat this as an engine time-evolution/design boundary, not a navigation failure. |
| First run-007 result extractor sent literal `\n` through SSH Python `-c` | 1 | Summary and other reads succeeded, but the extractor parsed no result. Switched to one known result path and a newline-free expression. |
| First time-scale diagnostic lost the Xvfb command through nested quoting | 1 | `xvfb-run` exited usage 2 before Unity started. Replaced nested quoting with a remote heredoc and pipefail. |
| Extended time-scale diagnostic lost its completed result in a quoted parser | 1 | Unity completed, but the downstream Python parser had escaped dict quotes. After repeated shell-boundary failures, moved summary construction into the diagnostic script itself. |

## Current Next Action

Execute Tasks 2-10 directly in the main agent. Keep all Gate A runs immutable, use `pre-discovery-run-015` as the frozen standalone-helper baseline, begin product RED tests immediately, and treat Gate B/real-API failures as visible evidence rather than a reason to defer the implementation or local commit.

## 2026-07-15 Gate A Run-003 Decision Gate

- Status: all-target visible-only scope and reset-snapshot role are explicitly approved. The one permitted implementation-plan review is complete, all five findings are dispositioned, and the final plan is locked at SHA-256 `f9f8f998a779754c538ff9a2e931ec1f463c58e1297d1a84a4489a4928c977ab`; product/helper source remains frozen until Gate A v3 passes.
- The v2 matrix adapter and completed-transaction evidence loss were repaired RED-first. Remote Python 3.11 compile, Ruff lint/format, probe 12/12, controller 14/14, and verifier 10/10 mutations plus 4/4 wiring checks pass.
- `discovery-run-003` retained the completed 27-action setup transaction but failed the first trial because one reset-invisible FloorLamp has no valid public discovery witness. No exact-case manifest was written.
- User-selected upstream rule: public semantic targets may use their frozen direct pose while offscreen unless a complete containment read proves a closed openable ancestor. A target inside a closed container returns non-terminal `target_not_visible` with zero backend action and no parent hint or automatic container action. The model explores/opens containers itself; repeated lack of progress eventually reaches the existing Agent limit and becomes Agent failure.
- New runtime fact from the user supersedes that rule: a target absent from the current camera view cannot be navigated to. The remaining scope question is whether this applies uniformly to receptacles such as Drawer/Cabinet/Shelf as well as Mug/FloorLamp.
- Scope is now explicit: every physical target, including Drawer/Cabinet/Shelf/CounterTop and Mug/FloorLamp, must be strict-visible in the current event before any navigation backend request.
- User approved retaining reset-time bounded scan/one frozen pose only as post-visibility execution data. The rewritten 1,614-line spec passes fence, placeholder, credential, stale-semantics and whitespace gates at SHA-256 `b1ee5ef4b402ab55bed793941c86c1b5f3fd900035db8142499b1f5b848c5fac`.

"""Test doubles for HomeMaster deterministic/mock providers.

These classes were moved from production code (pipeline/stage_runtime.py and
stages/executor.py) during the Phase 1 deterministic cleanup.  They exist
solely for offline/unit testing and must never be imported by production code.
"""

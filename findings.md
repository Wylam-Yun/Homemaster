# ALFWorld Harness Findings

## 2026-07-12 Initial State

- Repository: `/home/haodong2/weilin/red_bird/Homemaster` on `hkust4`.
- Branch: `visualagentloop`, tracking `origin/visualagentloop` at `98c3908`.
- Existing dirty state: modified `.gitignore`; untracked `docs/record/` and `plan/V1.7/`.
- No pre-existing root planning files were present.
- Required source documents and failed episode are named in the user-provided task; their contents are not yet assessed.

## Authoritative Design State

- The evaluation spec says the design direction and fifteen listed decisions were already confirmed by the user; implementation remained forbidden only because independent review and real-environment gates were deferred.
- The selected architecture is one public `robot_manipulate` backed by a shared executor and a `put` action profile. The upstream decision eliminates per-action public tools and per-action retry loops while preserving future `take/open/toggle` profiles.
- A `put` call locks the held objectId, target receptacle objectId, `PoseContext` candidate list, and order once. It starts from the current pose, retries only after an explicit THOR failure with fully unchanged relevant state, and stops at the first return-code plus terminal-state success.
- Success requires all three: THOR success, the exact object leaving inventory, and the exact object's parent containing the exact receptacle. Contradiction or partial change is `execution_state_uncertain` and stops immediately.
- Explicit numbered targets are strict. A missing `pencil 2` is `target_not_found`; it must never be normalized and reparsed into `pencil 1`.
- Harness grounding/navigation/operation/uncertain/unclassified failures terminate the Episode, do not increment model invalid-action count, and are excluded from Agent scoring while remaining visible in Harness coverage.
- `robot_inspect_view` is removed rather than repaired; model-visible output must include stable error, inventory, object state, state change, raw THOR detail, and latest image without objectIds, poses, scene inventory, or expert answers.

## Root-Cause Evidence Already Captured by Specs

- Navigation currently allows a 2D detection to override `metadata.visible=false`, producing a false `robot_go_to` success.
- Tool grounding can pass an unresolved explicit label to the adapter, whose second parsing strips the instance suffix and selects another instance.
- Internal manipulation results contain richer state, but the visual projection collapses most failures to `action_failed`.
- The failed trace established correct first `put` semantics and a real THOR failure with unchanged inventory and goal; it did not establish that the target Shelf is permanently unavailable.

## Failed Episode Direct Inspection

- `summary.json` reports `benchmark_env_step_limit`, 50 environment steps, 37 invalid actions, goal rate 0.0, and no win.
- The model first navigated to `desk 1`, took `pencil 1`, navigated to `shelf 1`, and issued the correct `put(pencil 1, shelf 1)` call.
- `robot_inspect_view` repeatedly returned the same prior frame path and no new backend step.
- After the put failure, the model visually misread the held pencil as placed, marked its own plan complete, and repeatedly called `robot_verify`; this is downstream model recovery behavior after the Harness failure/feedback loss, not evidence that the initial semantic action was wrong.
- The latest commit `98c3908` is a large objectId manipulation change touching adapter, prompt, registry, tools, and tests; its 2,333-line scope is relevant to the double-resolution regression investigation.
- Remote JSONL files are valid JSON-per-line. `jq` is unavailable, so subsequent structured extraction will use Python standard-library JSON parsing.
- Exact frame hashes prove `frame-0003.png`, `frame-0004.png`, and `frame-0005.png` are byte-identical across Shelf 1 navigation and both failed puts. The tool did not create a changed visual terminal state.
- The actual `model_trace.jsonl` manipulation record contains only a text block such as `{"error":"action_failed","success":false}` plus the image path; its rich internal `data` is absent. This is the authoritative model-visible projection, not merely an internal helper assumption.

## Independent Root-Cause Review

- The single most likely root cause is a cross-component contract mismatch: navigation accepted a render/detection pose, while direct THOR manipulation assumed that pose was sufficient and attempted exactly one put. The exact Shelf rejected the action.
- Orthogonal external-state evidence for the failed puts is consistent per instance: THOR error detail, exact Pencil still in inventory, goal still `0/1`, and unchanged RGB frame. Internal labels such as `Reached shelf 1`, `invalid_action`, or `action_failed` are not external terminal-state proof.
- A working control in the same benchmark run (`BaseballBat -> Bed`, episode 0009) used the same direct objectId/PutObject plumbing and ended with empty inventory, goal `1/1`, and `won=true`. This rules out a globally unusable PutObject API and isolates receptacle/pose feasibility.
- The initial Harness failure is distinct from later model recovery failure and from the late `pencil 2 -> pencil 1` grounding drift. The latter is a real secondary Harness defect but did not cause the first put rejection.
- Evidence still missing from the historical run: raw complete THOR events, per-candidate poses and visibility facts, exact parent/child receptacle fields, and an old-commit A/B run. Those claims must be established by new real-environment evidence rather than inferred from trace prose.
- The task directory contains three trial subdirectories; the exact historical trial must be identified before any environment reset or experiment.
- Object-pose comparison identifies the historical trial as `trial_T20190908_122154_042763`: its single Pencil pose `(-1.57117891, 0.882609248, 0.832937)` matches the held objectId recorded in episode 0006.

## First Independent Design Review

- Verdict was `FIX`; no product implementation was approved.
- All eleven findings were dispositioned in evaluation spec section 20.11. Ten were accepted directly. The recommendation to prefilter exact targets or elevate selection to a semantic Shelf type was not adopted because it conflicts with the user-confirmed rule that the model selects the exact instance and Harness never swaps Shelf.
- The revised design now includes exhaustive move/put/read transitions, separate navigation and operation contexts, a deterministic scene index, an authoritative terminal outcome gate, explicit legacy-action boundaries, safe raw-detail projection, all-six-Shelf experiments, fixed navigation/operation budgets, richer evidence events, exhaustive scoring outcomes, taskset propagation, and the expanded UNVERIFIED symbol list.
- Navigation spec section 14 now requires its own candidate/backend-action/time budget and actual-pose evidence.

## Current Code Boundary Evidence

- `tools._exec_go_to()` grounds a label, then calls `adapter.go_to_target()` with the grounded string. The adapter calls `_resolve_navigation_target()` again, so object selection is not authoritative-once.
- `env_adapter._object_query_key()` strips every trailing digit after normalization. An exact miss on `pencil 2` can therefore fall through to type-level Pencil matching and deterministic selection of another instance.
- `_target_visibility_score()` initializes from exact-object `metadata.visible`, then sets `visible=True` whenever any target id has an `instance_detections2D` entry, even if the metadata value was false. This directly implements the observed navigation false positive.
- `_teleport_to_targets()` returns as soon as that combined boolean is true. It does not separately require external teleport success, exact metadata visibility, and a positive-area exact-object detection.
- `manipulate_with_thor()` increments `AlfworldEnvState.invalid_action_count` for any backend manipulation failure. This violates the required model/Harness accounting once a semantically correct put exhausts Harness execution.
- `_visual_tool_result()` emits only `{success, error}` for manipulation failure, and `_visual_error()` maps nearly every non-validation/non-verify failure to `action_failed`, discarding the model-visible inventory/object-state/detail contract.
- Current navigation candidates are generated from up to 12 nearest reachable positions with deterministic rotation/horizon lists, but candidate generation depends on still-UNVERIFIED `GetReachablePositions` behavior.
- Runtime event counts show 69 tool calls in the failed Episode, separate from the 50 ALFWorld environment steps, confirming that the current trace already distinguishes model/runtime tool traffic from environment-step limits at a raw-event level.
- `AlfworldEpisodeResult` and `AlfworldSummary` currently expose only one failure reason, environment steps, invalid actions, and success rate. They cannot yet represent score eligibility, Harness failure classes, backend action counts, or Harness coverage.
- `AlfworldBenchmarkRunner._stop_condition()` only stops for won, invalid-action limit, environment-step limit, or environment done. There is no immediate Harness-failure stop signal.
- The THOR `put` path resolves the target from a string again, takes whichever object is currently first in `inventoryObjects`, calls `PutObject(forceAction=True, placeStationary=True)`, and trusts only `lastActionSuccess`; it does not verify exact held-object identity or terminal parentage.
- `model_trace.jsonl` is not schema-identical to `runtime_events.jsonl`: model tool records have a top-level `name` but no `type`, while runtime events use `type`. Future assertions must parse each source's actual schema separately.
- Repository-wide search found no persisted raw per-Shelf experiment artifact outside the prose specs/issue record. Existing tests contain mocked external fields and therefore are not authoritative evidence for THOR runtime contracts.

## Real-Environment Gates At Initial Discovery

- `GetReachablePositions` request/return/failure contract.
- `parentReceptacles` and `receptacleObjectIds` exact formats and event timing.
- `instance_detections2D` bbox format/coordinates/positive-area rule and correspondence to the same event RGB.
- `event.frame` availability, pixel format, and saved-image identity.
- Any interactive-pose query API.
- Exact `open` and `toggle/use` terminal-state fields and values.
- Per-Shelf fixed-order candidate experiments must determine candidate count, backend action count, and wall-clock budget; every known positive instance must pass independently.

## Runtime Environment Discovery

- The default remote interpreter is `/usr/bin/python3` 3.10.12, while HomeMaster declares Python `>=3.11`; it is not an acceptable project test/runtime interpreter.
- No `conda`, `uv`, `python3.11`, or `python3.12` command is currently on the non-interactive SSH PATH, and no virtual environment was found under `/home/haodong2/weilin/red_bird`.
- The ALFWorld source tree is `/home/haodong2/weilin/red_bird/alfworld` with `configs/base_config.yaml` and project metadata present.
- Three user-owned long-running Xvfb servers exist on displays `:99`, `:100`, and `:101`. Their health/authentication and the Python environment used by prior ALFWorld runs remain unverified.
- No THOR/Unity episode process was active during inspection.
- The dedicated environment is `/data0/yuqiao/envs/hm_alfworld/bin/python`, Python 3.11.15.
- Installed runtime versions: ALFWorld 0.5.0, ai2thor 2.1.0, NumPy 2.4.6, OpenCV 5.0.0.93, Pillow 12.3.0, HomeMaster 0.1.0, PyYAML 6.0.3.
- `alfworld` and `homemaster` import from the current `/data1/haodong2/weilin/red_bird/...` source trees (the target path resolves into the same storage), while ai2thor imports from the dedicated environment.
- `DISPLAY=:99` with `/tmp/xvfb-run.fRevQI/Xauthority` passes `xdpyinfo`, proving the X server is currently reachable.
- `python -m pip check` is not clean: it reports `textworld 1.7.0 is not supported on this platform`. No dependency mutation has been made.

## Direct THOR Runtime Contract Evidence

- Evidence roots: `var/alfworld-evidence/20260712-preimplementation/runtime-contract`, `runtime-contract-v2`, and `runtime-contract-v3`. Each run used a fresh controlled `xvfb-run`; the new Unity/Xvfb processes exited cleanly.
- The direct probe imports ALFWorld/ai2thor but no HomeMaster implementation or resolver. Process exit code was 0 for all three runs.
- Reset resolves exactly six Shelves in deterministic objectId order matching historical labels. Reset return code is successful; inventory is empty and goal is `0/1`.
- `GetReachablePositions` succeeds and returns the same 121 positions in both `metadata.actionReturn` and `metadata.reachablePositions`. Both representations normalize identically. The pre-action reset event has an empty `reachablePositions` list.
- Passing `gridSize="invalid"` is silently ignored: the action still succeeds with the same 121 positions. The current runtime therefore does not provide parameter-level failure semantics for this query.
- A `TeleportFull` to the first returned position succeeds. The actual event agent pose matches requested x/y/z/rotation/horizon within the recorded float tolerance, and the state vector remains unchanged.
- Candidate external pose fields are present as `metadata.agent.position`, `metadata.agent.rotation.y`, and `metadata.agent.cameraHorizon`; their exact request/actual contract is now observed for one pose but still requires per-candidate coverage.
- `event.frame` is `uint8` with shape `[300, 300, 3]`. PNG save/reload preserves exact pixels and hash. Exact Shelf detections are `[x1, y1, x2, y2]` lists with positive areas in the same event.
- UNVERIFIED probe action `GetInteractablePoses` raises `ValueError('Invalid action: GetInteractablePoses')`. The current design must not depend on it.
- Replaying the exact expert prefix picks up only `Pencil|-01.57|+00.88|+00.83`. The expert `PutObject` request with exact object/receptacle IDs, `forceAction=true`, and `placeStationary=true` returns success; inventory becomes empty; goal becomes `1/1`; Pencil parents contain exact Shelf 4; Shelf 4 children contain the Pencil.
- On successful put, `parentReceptacles` retains the original Desk and adds Shelf 4 rather than becoming a single-parent list. The terminal gate must test membership of the exact Shelf, not equality to a singleton.
- Two independent runs produce identical reset, pickup, and put frame hashes and identical exact terminal states.
- A visible Drawer probe returns `lastActionSuccess=true` and changes exact `isOpen` from false to true. This verifies the open state field/value/timing for that target in this runtime.
- Hidden toggleable targets return `lastActionSuccess=false`; raw `errorMessage` includes exact objectId, coordinates, and a Unity stack trace. This is direct evidence that raw detail can leak prohibited data. No successful `isToggled` transition has yet been observed, so toggle remains UNVERIFIED.
- Failure events may have `errorCode=null`; a failed `OpenObject` on Blinds even had an empty `errorMessage`. Stable classification must use return status plus terminal state, never require a nonempty error code/detail.

## Repository Documentation Gap

- `docs/pitfalls.md` does not exist. If this repair confirms a non-obvious pitfall, delivery must create it with the newest entry first.

## Evidence Rules

- Record external or runtime observations here, not in `task_plan.md`.
- Distinguish code-path/trace evidence from authoritative THOR return codes and terminal world state.
- Report each target instance independently; never pass an aggregate because one instance succeeded.

## Characterization Implementation Review

- The pre-execution review verdict was `FIX`; the six-Shelf characterization must not run until every blocker below is closed.
- Timeout isolation: accepted. Each Shelf must execute in a killable process boundary; a timed-out THOR call may not remain alive while another Shelf starts.
- External reads: accepted. Return status, metadata, pose, inventory, exact Pencil/Shelf, parent/child state, detections, frame identity, reachable results, and goal reads must preserve `ok/error/missing`; missing evidence cannot become an empty collection.
- Failure provenance: accepted. External uncertainty, script defects, and artifact failures remain distinct in summaries and exit status.
- Per-instance gates: accepted. Navigation must pass separately for all six Shelves; put must pass separately for known-positive Shelves 3, 4, and 6. Other put outcomes remain visible and cannot be hidden by `any`/best aggregation.
- Budget scopes: accepted. Navigation and operation each require candidate, backend-action, and wall-time limits with explicit stop reasons and terminal-priority ordering.
- Unified evidence calls: accepted. Reset/load/goal boundaries and every prefix, reachable, teleport, and put request require timing and raw evidence or an explicit missing/exception record.
- Local context identity: accepted. The locked local pool must include the current pose first and bind its hash to the successful source event/frame/raw artifact, anchor, and actual pose.
- Numeric canonicalization: accepted. Reachable coordinates must be finite, rounded into the stored canonical value, and normalize negative zero; canonical JSON rejects NaN and Infinity.
- Two-stage budget selection: accepted. Characterization uses predeclared safety ceilings over the complete locked pool, derives production limits by a fixed recorded rule, then validates known-positive instances again with those derived limits. One CLI cap cannot prove itself.
- Helper batch completed: `independent_alfworld_probe.py` now rejects nonempty output directories, refuses artifact overwrite, records both canonical-content and persisted-file SHA256 values, rejects non-standard JSON floats, writes a raw event for every returned `thor.step`, and exposes tri-state `external_read`. Exceptions and missing events retain `raw_event=null` and produce a nonzero probe result unless explicitly expected.
- The separate characterization state-machine batch owns process isolation, per-Shelf summaries/gates, context pools, and two-stage budgets; the helper batch does not claim those items complete.

## Probe Negative Review Follow-Up

- A synthetic returned event with `metadata=None` exposed a projection-layer crash after `external_read` had correctly classified the event as missing. The projection helpers must never re-read `event.metadata` unsafely; otherwise an external missing-state result is misreported as a script failure.
- An exact-target detection entry with malformed bbox `['bad']` previously left `external_read.status=ok`. Existing detections now require exactly four finite numeric values and a computable area. A missing exact-object detection remains a valid observation result with `detected_2d=false`, not a missing read.
- Frame evidence is authoritative only for a nonempty `uint8` RGB ndarray. Wrong dtype, rank, channel count, or empty spatial shape is an external read error. Each exact Pencil/Shelf observation now records metadata visibility, bbox presence/area, frame validity, and the combined strict observation result.
- `metadata.lastAction` is part of the return contract. Missing, malformed, or request-mismatched action names are errors, preventing a stale successful event from satisfying `require_success`.
- Probe process success now depends on external terminal-state gates, not trace presence: expert put requires exact inventory/parent/child plus goal `1/1`; open requires exact `isOpen: false -> true`; toggle remains `UNVERIFIED` and makes the combined probe nonzero until an exact successful toggle transition is captured.
- The Teleport contract now selects a reachable point with nonzero distance from the source pose and requires returned success, tri-state read success, requested/actual pose agreement at the observed tolerance, and actual nonzero movement.
- Pre-product-change ALFWorld test baseline: 49 relevant tests pass and one pre-existing Runner test fails because it expects `robot_navigate` while the current registry does not expose that tool. The repository-wide ALFWorld Ruff baseline has 37 pre-existing errors. Neither baseline is attributable to this ignored evidence-script change.
- The first main-agent negative-test attempt incorrectly used a remote repository path as a local tool `workdir` and failed before running. The corrected test used SSH with Python source on stdin and ran successfully; future remote negative tests must use that established invocation.

## Final Characterization And Production Budgets

- `shelf-characterization-v3` completed with controller status `pass`, no hard-gate failures, and zero hard failures.
- All six Shelf exploration instances independently passed navigation, Put return status, exact inventory/`isPickedUp`/parent/child terminal state, and goal `1/1`; no best/any aggregation was used.
- Exploration navigation candidates/backend actions were: Shelf 1 `51/52`, Shelf 2 `57/58`, Shelf 3 `57/58`, Shelf 4 `3/4`, Shelf 5 `1/2`, Shelf 6 `1/2`.
- Exploration local put candidates/backend actions were: Shelf 1 `2/3`, Shelf 2 `3/5`, Shelf 3 `1/1`, Shelf 4 `1/1`, Shelf 5 `2/3`, Shelf 6 `1/1`.
- The predeclared derivation rule selected navigation `65 candidates / 66 backend actions / 34804 ms` and local put `9 / 17 / 5669 ms`.
- Shelf 3/4/6 all passed a second production run under those exact budgets.
- Evidence: `var/alfworld-evidence/20260712-preimplementation/shelf-characterization-v3/summary.json` and `production_budget.json`.

## Product Harness Black-Box Gate

- Three independent fresh-Xvfb processes used the product `AlfworldEnvAdapter`, not the independent characterization state machine, to run `go_to(pencil 1) -> take -> go_to(exact Shelf) -> put` for Shelf 3/4/6. Every process exited 0.
- Product navigation backend actions were Shelf 3 `58`, Shelf 4 `4`, Shelf 6 `2`; each stayed within the fixed production budget. Every put succeeded on the current pose with one backend action.
- An independent terminal parser asserted THOR return success, Pencil absent from inventory, `isPickedUp=false`, exact Shelf membership in Pencil parents, Pencil membership in exact Shelf children, goal `1/1`, and saved navigation/put PNG pixels equal to their final THOR events for each instance.
- The same three gates were repeated after the final structured-trace and strict `isPickedUp` changes; all three fresh-Xvfb processes again exited 0 with identical pass criteria.
- Final evidence: `var/alfworld-evidence/20260712-preimplementation/product-harness-v2/`.

## Delivered Product Boundary

- `robot_go_to` now uses authoritative exact-object grounding, locked candidates, actual-pose verification, strict exact visibility/bbox gates, fixed budgets, and final-event image delivery.
- `robot_manipulate(action=put)` now uses the locked `PoseContext`, a common execution core, exact preconditions, fixed local budgets, exhaustive return/state classification, and exact external terminal gates.
- Harness terminal outcomes stop ordinary Episodes and long-horizon tasksets, exclude infrastructure failures from Agent scoring, mark remaining subtasks not-run, and report valid coverage/formal-score availability.
- `robot_inspect_view` is absent from registry and prompt. Put feedback exposes stable minimum state and deterministically redacts forbidden THOR detail.
- Toggle/use remains UNVERIFIED for successful terminal transitions and is not part of the put MVP claim.

## Shared-Workspace Sync Incident

- Uploading a stale local `types.py` briefly removed a newer taskset constant and fields from the remote dirty worktree. Collection failed immediately on the missing import, so no false-green result escaped.
- The taskset patch owner reconstructed the lost delta on top of the latest file, preserved the new backend counter, and passed outcome/runner/CLI tests. The remote file was then copied back to staging before further edits.
- Shared remote synchronization must compare ownership/current file state before upload; a local dirty clone is not automatically the authoritative copy.

## Final Verification

- Focused ALFWorld benchmark suite: `121 passed`.
- Full repository suite after generated-cache cleanup: `352 passed, 1 skipped`; the skip is the opt-in live ALFWorld smoke test. The only warning is the existing `jieba` import of deprecated `pkg_resources`.
- Touched-file Ruff check and Ruff format check pass; compileall, cleanup guard and `git diff --check` pass.
- JSONL smoke independently read the persisted events and confirmed ordered internal execution records retain objectId/raw/pose evidence while the model-visible projection recursively excludes those fields.

## 2026-07-13 V1.8 Design Continuation

- Canonical repository state is the clean `visualagentloop` branch on `hkust4` at `0fdfeaa00b921d8ea347655ecbd4c32b9ff30d6d`; the older local V1.7 staging tree is not authoritative.
- The real 10-Episode run produced raw success `5/10`, Agent success `5/6` on score-eligible Episodes, and Harness coverage `6/10`; formal score was unavailable.
- Exact-trial probes established three separate Harness defects: navigation successes beyond the 65-candidate budget, visible-but-inoperable Drawer poses, and model feedback reading the wrong integration layer. A provider SSE ordering failure was also misclassified as Agent failure.
- The user selected one upstream architecture: use ALFWorld's generic Oracle receptacle map for low-level poses, remove navigation/Put pose search, keep hidden movable-object exploration under model control, require the exact requested target to be visible after navigation, use single-shot Open/Put, project only typed execution feedback, and exclude Provider/runtime/Harness failures from Agent scoring.
- Standard Oracle source/data references resembling `controller.receptacles[exact_object_id]["locs"]` were observed in the installed environment, but direct HomeMaster integration and per-instance terminal-state behavior remain `UNVERIFIED`. Review may require the Phase 0 gate but must not endorse these symbols.
- V1.8 is design-only. No product implementation is authorized until independent review findings are dispositioned, the user approves the revised spec, and Phase 0 verifies runtime return codes plus external terminal states per instance.

### Main-Agent Self-Review Candidates

- `AlfworldExecutionFeedback` does not currently declare `success`, while the required model-visible JSON does. If feedback is the sole authority, success cannot be re-created downstream from `error` or a second generic layer.
- `action_not_applicable` appears in the Open/Close state flow but is absent from the exhaustive classification table, leaving terminal/scoring/backend-action semantics undefined.
- Context freshness is underspecified after a non-moving state action. Open must be able to refresh/rebase the same locked Oracle execution context for a following Put; treating every newer event as stale would force an unnecessary second navigation, while accepting arbitrary event drift would use stale proof.
- A visible movable may expose multiple parent memberships. The design says to choose a visible Oracle-capable anchor but does not yet define a deterministic unique rule or an explicit ambiguity failure; selecting a set-dependent first value would violate target-locking discipline.
- A successful Oracle move followed by exact-target visibility failure needs an explicit terminal Harness classification distinct from the pre-move, zero-action `target_not_visible` response.
- Provider/runtime availability and Harness coverage are conceptually separate. The current wording says a Provider failure lowers `coverage` while the formal-score gate is named `harness_valid_coverage`; the metric and denominator need one unambiguous definition.
- Current `harness_valid_coverage` is implemented as `score_eligible / total`, so Provider/runtime/artifact/cancelled failures are all counted as “Harness invalid.” The revised design should preserve a total formal-score eligibility gate while reporting separate Harness coverage and Provider/runtime availability, or explicitly rename the aggregate metric.
- The Runner receives `GenericAgentRuntime`'s actual string error code `transport_error`. `LLMProviderError` is an exception class and `provider_error` is a provider-level error type, not necessarily values that reach `_episode_classification()`. Normalization must be specified at one concrete boundary and tested with the real `RunResult` shape rather than a list of unlike symbols.
- The public registry also exposes `robot_find_object` and `robot_navigate`. Replacing only `robot_go_to` would leave possible navigation/hidden-object bypasses; the design must state whether these entry points delegate to the same Oracle executor, are removed, or remain under a proven non-moving contract.
- This is a concrete bypass, not a naming concern: `tools._exec_find_object()` calls `env_adapter.find_object()`, whose `_search_visible_object_source()` iterates known receptacles and sends environment navigation commands until the object appears. That behavior directly violates the approved hidden-object boundary and must be removed from the formal path with registry/call-boundary tests. `robot_navigate` separately reaches the legacy environment command path and must not remain an alternative low-level navigation mode.
- `robot_manipulate` supports take/open/close/put/heat/cool/clean/slice/use. The design's global claim that all model-visible execution state comes from typed feedback is broader than its Open/Close/Put flow and integration-test matrix. It must either define typed feedback for every public robot action or narrow the invariant and explain the unaffected actions without permitting generic result inference.
- The design must make safe `detail` construction explicit. Regex-redacting a free-form THOR string is not a complete security boundary; model-visible detail should come from allowlisted templates while raw external detail stays internal.

### Independent V1.8 Review Result

- The independent design verdict was `FIX`; a separate read-only code-boundary audit agreed. Neither agent changed files or endorsed external runtime symbols.
- All ten design findings and five boundary-audit supplements were accepted. The revised spec now includes old navigation bypass removal, typed tri-state feedback, two-stage Gate A/B verification, a context rebase state machine, deterministic anchor failure, exhaustive tool/Episode taxonomies, all-action coverage, distinct counters/metrics, auditable provider retry, exact Take locking and full deletion/synchronization boundaries.
- The feedback and Oracle integration remain design claims only. No product code was modified and all V1.8 external compositions remain `UNVERIFIED` until the corresponding true-environment gate passes.

### Revised-Spec Self-Audit

- Initial Markdown/diff check passed with no whitespace errors.
- Corrected four contract-level wording defects found after disposition: the old 6/10 mixed metric is now explicitly historical, the summary covers the full action gateway rather than only Open/Put, typed feedback uses `terminal=true + classification` correctly, and Dispatcher `invalid_tool_arguments/unknown_tool` are included in the closed nonterminal error map.

## 2026-07-13 V1.8 Implementation Authorization

- The user explicitly approved implementation after design commit `22cb122e1b186c8e3877cd6504f805685e1bbfc7`; the prior waiting-for-approval state is obsolete.
- The canonical `hkust4` worktree is clean on `visualagentloop`; local HEAD and `origin/visualagentloop` both equal `22cb122e1b186c8e3877cd6504f805685e1bbfc7` at recovery time.
- The implementation-plan review may run exactly once, and the final code review may run exactly once. Architecture review must not be reopened.
- All V1.8 ALFWorld/THOR symbols and their HomeMaster composition remain `UNVERIFIED` until the applicable Gate A/Gate B per-instance black-box evidence passes.
- A fresh local isolation clone named `homemaster-v18-impl-20260713` was created from the canonical remote solely to support patch-based editing; the prohibited stale `homemaster-v17` and `homemaster-v18` trees are not used.
- Before any V1.8 product change, the focused remote baseline passed `145` tests in `2.11s` across `tests/homemaster/benchmarking`, ALFWorld CLI, GenericRuntime and ToolDispatcher. This is an internal regression baseline only, not Gate A evidence.
- The historical run persisted only task-directory Episode IDs and discarded exact `trial_*` fingerprints. ALFWorld `AlfredThorEnv.seed()` only stores `self.random_seed`; `reset()` selects with an unseeded module-global `random`, and controller initialization consumes that same global RNG. A static seed-42 replay matched `0/6` known exact trials, so deterministic replay is disproven.
- Six historical trials are uniquely recoverable from exact object evidence. Episodes 0001/0003/0007/0009 each retain three indistinguishable candidates. Gate A/B must cover the 18-trial candidate-complete superset rather than guessing; the fixed ten-task API manifest must label four chosen trials as deterministic replacements.
- The real Runtime also emits `max_consecutive_tool_errors`; the approved closed runtime-code table omitted it. It is an Agent loop-guard failure and is now explicitly mapped to `agent_model_failure` before implementation-plan review.
- All 20 locked Gate A/B trial paths in the implementation-plan appendices exist on `hkust4`. Six connection-closed results from an over-parallel SSH check were transport failures; sequential rechecks passed, so none was misreported as a missing dataset file.
- The one allowed implementation-plan review returned `FIX` with ten findings. All were accepted: gateway-only navigation, frozen exact case manifests, lifetime/AST import isolation, complete goal/inventory/frame gates, status-aware runtime classification, cause-aware Provider retry, TextWorld typed mapping, separate taskset not-run state, injected Dispatcher observer, and Gate B source/runtime identity.
- No external ALFWorld/THOR/SDK symbol was endorsed by the plan review. All remain `UNVERIFIED` until their live cases pass.
- The completed Gate A controller independently proved its orchestration behavior against fake workers: 20/20 discovery processes and 166/166 frozen cases, including 86 positive and 80 fixed-code negative cases, passed; timeout cleanup removed the worker and its child before another launch. This validates only controller mechanics, not ALFWorld/THOR feasibility.
- The controller/independent-verifier contract now requires a per-case `initial_state` artifact in addition to lookup, navigation, action, terminal-state, raw-event and frame evidence. This prevents hidden/inventory zero-action cases from passing without an external before-state record.
- The failed minimal Oracle smoke did not fail at reset or `TeleportFull`. Phase-marked evidence shows reset completed, the returned action was `TeleportFull`, `lastActionSuccess=true`, and actual pose matched the request. Failure occurred at result serialization because `event.instance_detections2D[target_id]` is a NumPy ndarray in this environment; the list/tuple-only bbox validator also incorrectly returned no area.
- A single-variable `/tmp` diagnostic normalized the four bbox numbers before validation/JSON. It exited 0 with exact target visible, bbox `[251, 232, 299, 300]`, area `3264.0`, and no Unity/THOR residue. This proves the smoke-helper root cause only, not Gate A.
- The trailing Unity `ArgumentNullException(name)` is a separate teardown event in the `before_close`/`after_close` time window. ai2thor 2.1.0 `Controller.stop()` sends an empty control payload in the observed environment. It must be recorded as teardown evidence and must not be used to relabel a successful move; other Player.log exceptions remain actionable.
- Main-thread helper composition audit found that the first controller version froze `exact-cases-v1.json` and immediately launched action workers. That leaves no independent verifier gate between read-only discovery and mutation, contradicting the approved two-stage evidence contract. Controller and verifier must expose separate discovery/run commands and the run command must consume the previously verified manifest hash without rediscovery.
- The first probe version loaded `movable_location_profiles` but only emitted explicit inventory/hidden labels and did not persist a canonical Oracle-map inventory. Consequently a missing surface/open-container/closed-container/floor case or skipped exact Oracle ID could survive controller checks. This is an internal false-positive risk; no live Gate A run has started.
- The first split controller fixture passed controller-local tests but the latest independent discovery verifier rejected its first frozen case because `expected.navigation_context_expected` was missing. This is direct evidence that same-owner fake success did not prove the shared schema. The acceptance gate is now cross-file: the latest controller artifact must pass the latest verifier in both discovery and run phases.
- Split v6 produced 198/198 case-level PASS rows, but the independent run verifier correctly rejected the overall artifact after the controller bytes changed to fix a wrong trial-hash summary field. Case semantics cannot override source-identity failure; all v6 evidence is invalid and must not be rehashed or reused.
- Final split v8 freezes the latest helper/fake bytes and passes the latest independent verifier in both phases: 20 discovery workers and zero case workers produce a 198-case manifest; zero discovery workers and 198 case workers then pass. The verifier deliberately still reports external symbols `UNVERIFIED`, because the fixture runtime is synthetic.

## 2026-07-14 First Real Gate A Discovery Failure

- The explicit real discovery phase stopped after its first trial, `episode-0001-candidate-1`, with worker exit `2`; cleanup completed, no action cases ran, and no frozen exact-case manifest was created. The failed evidence remains under remote `discovery-run-001`.
- Direct raw-event parsing, not the controller summary, confirms `metadata.sceneName` is `FloorPlan219_physics`, with 61 objects and 15 exact 2D detections. The matrix/trial identity expects `FloorPlan219`; ownership of this naming difference is still under investigation.
- The persisted movable artifact contains four exact Statue objects. Each has a reciprocal Shelf or SideTable parent with an Oracle entry, but each is `strict_visible=false`, so all four surface entries are `eligible_for_case=false`; every movable profile count is zero.
- The persisted Oracle artifact contains 24 total entries and 20 relevant entries, all for declared navigation/receptacle types. It contains no FloorLamp entry, so the trial cannot currently create the required positive `use` profile.
- The raw reset contains exactly two FloorLamp objects. Both are `toggleable=true`, `isToggled=false`, `visible=false`, have no 2D detection, and have no Oracle entry. Thus the failure is not "FloorLamp absent" or "toggleable field unsupported"; it is specifically an observation/pose-coverage boundary.
- The raw reset contains exactly four Statue objects. All are `pickupable=true`, `isPickedUp=false`, and reciprocally contained by a Shelf or SideTable. Three have no reset detection; one has bbox `[102, 17, 115, 41]` but still has authoritative metadata `visible=false`. The strict visibility conjunction therefore rejects all four without relying on bbox alone.
- The first attempted direct `traj_data.json` read used the repository root plus the matrix-relative path and failed because that dataset is mounted/configured elsewhere. The next evidence step is to derive the real data root from the recorded process/runtime configuration.
- The recorded worker command resolves that path unambiguously: `--alfworld-root /data1/haodong2/weilin/red_bird/alfworld`, Python `/data0/yuqiao/envs/hm_alfworld/bin/python`, ALFWorld `0.5.0`, ai2thor `2.1.0`. The exact trial SHA-256 was checked before reset.
- The helper's `_oracle_map()` reads only `worker_env.controller.receptacles`; this real object is a 24-entry mapping in the first trial. Whether the external controller intentionally limits this map to receptacles is being checked against installed source before assigning root cause.
- The helper data flow is deterministic: `_strict_visible()` requires metadata `visible is True` and positive bbox area; `classify_semantic_movables()` marks surface/open-container/floor entries ineligible when that reset-time conjunction is false; `discover_cases()` separately considers non-Oracle toggleables only when initially strict-visible and then requires `_unique_anchor()`.
- Consequently, all four Statue cases are filtered before action execution, while both FloorLamps are filtered before anchor resolution. This explains the missing cases mechanically but does not yet establish whether the contract or the trial selection is wrong.
- Installed ALFWorld source closes the Oracle-map question: `OracleAgent.explore_scene()` only inserts objects whose type belongs to `STATIC_RECEPTACLES`. A real-environment membership query confirms FloorLamp, DeskLamp, and Statue are all `OBJECTS` but none belongs to `RECEPTACLES` or `MOVABLE_RECEPTACLES`. Their absence from `controller.receptacles` is therefore the external API's intended scope, not a transient scan miss.
- The exact trial's authoritative trajectory SHA matches the matrix and locks `Statue|-00.19|+01.11|+01.19` plus `FloorLamp|-00.65|+00.27|+04.43`. Its expert high-level plan contains navigation coordinates for those targets, but the approved V1.8 boundary forbids expert-trajectory fallback; those coordinates are identity evidence only, not an acceptable runtime pose source.
- The four Statue parents are structurally unique and Oracle-backed, but all parent metadata also says `visible=false`; two parents nevertheless have positive bboxes. This reinforces that bbox-only visibility would reproduce the known false-positive class and cannot repair discovery.

### Scene Identity Root Cause

- ALFWorld `0.5.0` reads `scene_num=219`, constructs the logical base name `FloorPlan219`, and passes it to ai2thor. Installed ai2thor `2.1.0` explicitly appends `_physics` before Reset unless already present; Unity then reports the loaded asset as `FloorPlan219_physics`.
- The probe compared these distinct naming domains with naked string equality. Exact trial-path binding and the successful reset/TeleportFull prove the intended scene loaded; the reported scene mismatch is a Gate A false positive.
- Orthogonal evidence covers 541 persisted real raw events: one reports `FloorPlan219_physics` and 540 report `FloorPlan308_physics`; none reports an unsuffixed or double-suffixed name. The client-advertised 120-scene list is also fully suffixed.
- The matrix's 20 trials independently satisfy `expected_floor_plan == traj.scene.floor_plan == FloorPlan{scene_num}`. The correct gate must verify that logical identity plus the version-scoped runtime mapping to exactly one `_physics` suffix, not blindly strip arbitrary suffixes.
- `FloorPlan10_physics` is advertised by the installed client but lacks a persisted real reset event. Its runtime usability and all not-yet-run per-trial mappings remain `UNVERIFIED` until Gate A reaches them.

### Design-Gate Disposition

- The real config has `controller.type=oracle` and `load_receps=true`. ALFWorld therefore loads the trial's precomputed `receps.json`; its 24 keys/entries exactly match the persisted Gate A Oracle artifact. The missing FloorLamp is not caused by incomplete live exploration or helper serialization.
- The current approved specification deliberately makes a parentless, non-map fixture return terminal `oracle_anchor_unresolved`, forbids distance/expert/candidate fallbacks, and says any Gate A linchpin failure returns to design. The first real trial has now exercised exactly that prohibited boundary.
- Changing only discovery eligibility or weakening per-trial profile counts would hide the product infeasibility: the eventual product would still have no pose source for the exact FloorLamp. Therefore no manifest/helper-only patch is acceptable.
- The separate scene-name defect remains a bounded Gate helper contract correction, but implementing it now would not make Gate A feasible and would violate the root-cause-first/design-approval gate.
- The upstream decision is whether pre-episode deterministic scene scanning may create one general exact-object pose snapshot for the whole trial. That decision determines whether V1.8 can preserve single-pose action-time execution without expert trajectory data or resurrecting action-time candidate modes.
- On 2026-07-15 the user selected option A: the Harness may perform one deterministic, fully audited scene scan after trial reset and before the model's first request, then restore the exact initial pose. Action-time execution must still consume one frozen pose and may not search, retry another pose, or read expert trajectory fields.
- The current Runner calls `adapter.reset()` before provider/profile/client construction and before the agent runtime starts. Inside `AlfworldEnvAdapter.reset()`, the external reset occurs first, then scene generation/event sequence/index/state/frame are initialized. The scan belongs inside this reset boundary after exact external reset and before the returned `step_index=0` state/frame; otherwise the Harness would expose a scan pose as the model's initial observation.
- The restored initial event must become event sequence zero and the only source of the model-visible initial frame. Scan actions require a separate setup-action ledger; folding them into model `env_step_count` or Agent invalid-action responsibility would misattribute Harness setup work.
- A general snapshot must separate pose availability from navigation authorization. If any object observed during the hidden pre-model scan becomes directly addressable, the Harness would use omniscient setup evidence to discover an initially invisible Statue/FloorLamp for the model and violate the approved exploration boundary.
- The conservative design is to keep current-event strict visibility as the authorization gate for movable/toggle/fixture targets. The snapshot then answers only "which single pose should be used after the model has visibly discovered this exact target". Receptacles remain directly addressable through the public scene vocabulary; hidden closed-container objects remain unauthorized and may use a reciprocal parent only after becoming visible.
- This policy still makes the parentless FloorLamp pose representable, but a real product path must first establish visibility through model-led receptacle exploration. Gate A may test the lower-level pose/action contract with explicit setup; Gate B must prove a public product sequence that satisfies the same visibility precondition without expert data.
- Installed ALFWorld layout data bounds one scan input: `FloorPlan10-openable.json` has 16 entries/15 unique poses, `FloorPlan219` has 26/20, and `FloorPlan308` has 13/13. The approved builder also adds one geometry pose per cache-missing addressable ID, so the exact per-scene setup count is `1 GetReachablePositions + N frozen scan Teleports + 1 restore`, not the static-basis count plus restore.
- These counts prove boundedness only. They do not prove any exact FloorLamp/DeskLamp/movable becomes strict-visible at one of those poses; Gate A must assert coverage and pose usability separately for every frozen exact target rather than infer it from a successful peer or the existing receptacle map.
- The existing closed responsibility taxonomy is sufficient: a scan Teleport rejection with independently unchanged world state is `harness_navigation_failure`; any return/actual-pose/world-state/restore contradiction is `execution_state_uncertain`. Adding a parallel "scan mode" classification would create unnecessary branches.
- The current Runner creates `EpisodeOutcome` only after `adapter.reset()` returns, so setup failure cannot yet become a typed Episode without an exception or partial artifact. The clean boundary is an `AlfworldResetResult` carrying either the initial state or terminal setup feedback, setup action count and evidence ref; Runner constructs trace/outcome first and makes zero provider requests on failure.
- `setup_backend_action_count` must be distinct from model-triggered `backend_action_count`, while an explicit total may sum them for resource reporting. TextWorld returns setup `not_applicable` and zero. Every Adapter/Fake implementation and summary/taskset/CLI consumer must enter the interface audit.
- Long-horizon `advance_goal()` replaces the task while preserving the loaded scene and accumulated world state. The snapshot should be built once per scene reset, not once per subtask. It remains bound to scene generation/trial fingerprint; each direct movable entry also carries its transform/containment signature and becomes stale after that object moves.
- Goal advance invalidates active execution context but need not rebuild static fixture/receptacle poses. A stale moved-object direct entry falls back only to the already approved current-visibility plus reciprocal-parent rules; it never triggers an action-time rescan.
- Option A is an explicit exception to the old blanket ban on pose enumeration. The revised invariant must say: setup may execute one scene-wide, task/target/action-independent frozen scan plan; request-conditioned or action-time search, early stop, success-driven pose switching, retry with another pose and fallback remain forbidden.
- Scan policy/config/exact-ID/cache inputs are frozen before the first setup action. The one reachable query is then verified; its canonical return freezes the complete Teleport plan/hash before the first scan move. The plan executes completely even if a task-relevant object is observed early and may only move/observe; Open/Toggle/Pickup or other state mutations are forbidden during pose selection.
- Snapshot identity separates stable physical `scene_reset_fingerprint/scene_generation` from changing `goal_trial_fingerprint/goal_generation`; taskset `advance_goal()` cannot stale every static entry merely because the goal trial changed.
- Pose freshness is narrower than full object state: Open/Use/dirty/temperature transitions do not change navigation pose and must not stale an entry. Gateway-proven Take/Put relocation gets explicit `relocated` status that permits current-visible unique-parent resolution; only untracked transform/containment or generation drift is `stale -> execution_state_uncertain`.
- "All-object snapshot" means every reset exact ID has one typed row, not that every row has a pose. Never-observed/closed-container objects remain explicit `absent`/`unobserved`; synthesizing a parent pose for them would violate the hidden-object boundary.
- A partial scan is never published. Every scan pose plus restore must pass its own return/actual-pose/state gate, then the snapshot is installed atomically; one failed setup request cannot be hidden by another pose covering the same object.
- Static analysis recommends one HomeMaster-owned same-environment reset transaction and one resulting pose authority. A full reachable lattice is far larger and reintroduces candidate-budget behavior; twin-env scanning adds RNG/state-equivalence and resource risks. Neither is an automatic fallback.
- Existing timing evidence suggests roughly 0.43-0.45 seconds per THOR action. A 13-20 pose basis plus restore is approximately 6-10 seconds per reset before evidence overhead, still UNVERIFIED. Gate A's many fresh-reset cases will therefore need process-level timeouts and potentially tens of minutes, but no acceptance threshold may use a best/any aggregate.
- The current ordinary Runner reuses an Adapter across Episodes. If setup restore is uncertain, that environment must be quarantined/closed rather than reused; the revised lifecycle should create or replace the Adapter per Episode or expose an explicit unusable state.
- A real V1.7 API run supplies a decisive coverage control. In episode 0002, exact FloorLamp navigation requested `(-1.5, 0.9010564, 3.75, rotation=90, horizon=30)` as candidate 1; two separate moves returned success, exact actual pose, `visible=true`, positive detection and bbox area `13455`.
- That complete pose is absent from both the exact trial's `receps.json` (24 entries/16 unique poses) and `FloorPlan219-openable.json` (26/20). A static-basis-only scan therefore excludes a known-good fixture pose and is not the recommended MVP.
- The same run later dispatched a real model `use floorlamp 1` through `thor_api` and ended won=true, but it did not persist the raw Toggle return or exact before/after `isToggled`. It proves product-path plausibility, not the V1.8 operation terminal gate; Toggle remains `UNVERIFIED`.
- The known-good FloorLamp pose was the old deterministic geometry generator's first candidate. This supports a bounded middle design: one verified `GetReachablePositions` query, all valid exact trial cache poses as inputs, and at most one canonical geometry pose for each addressable exact ID lacking an exact cache pose. The complete union is deduplicated/frozen before any Teleport.
- Snapshot construction may use multiple deterministic inputs, but the published `OraclePoseSnapshot` is the sole action-time authority. `receps` and geometry never remain callable fallback stores; a malformed cache is not masked by geometry, and an action failure never selects another scan observation.
- After the full scan, each exact ID publishes one typed row using a fixed ranking over strict-visible observations: exact-cache provenance first, exact geometry provenance second, then incidental observation; within one source rank use descending bbox area and canonical pose tuple. The independent verifier recomputes this per exact ID; no cross-instance aggregation is allowed.
- The user explicitly accepts `harness_navigation_failure` when no usable pose exists. Missing coverage must remain visible and score-ineligible; it must not trigger runtime pose search or be attributed to the Agent.
- Parent anchoring is deterministic resolution before any external action, not a retry after a direct-pose failure. It is allowed only when the exact target is currently strict-visible, the current event proves reciprocal containment, exactly one innermost parent remains, and that parent has one current valid snapshot pose.
- Once the parent anchor is locked, its pose is the only pose for that navigation. If the Teleport succeeds but the exact child is not strict-visible with a positive bbox in the returned event, classify `oracle_target_not_visible -> harness_navigation_failure`; do not try another parent, an outer ancestor, the child's direct pose or a new geometry pose.
- No/ambiguous parent or absent parent pose is a zero-action `oracle_anchor_unresolved/oracle_pose_missing -> harness_navigation_failure`. A rejected Teleport with known unchanged state is `oracle_navigation_failed -> harness_navigation_failure`; malformed/stale reads, pose mismatch or unknown/changed state are `execution_state_uncertain`.
- The user approved starting the design-only delivery on 2026-07-15. The existing V1.8 spec remains the Markdown truth source; no separate duplicate design document will be created. Product/helper implementation remains blocked until this revised spec is committed, user-reviewed and the replacement Gate A passes.
- Spec mapping found the old pose-source assumption is cross-cutting, not a single paragraph: the decision summary, goals/non-goals, alternatives, external symbols, `OraclePoseStore`, anchor resolver, navigation semantics, counting, observability/security, migration guards, Gate A/B, implementation order and final invariants all still name a direct receptacle map or a blanket ban on pose enumeration. They must change atomically to the approved setup-scan exception and sole published snapshot authority.
- Existing Gate A wording also assumes discovery can be action-free. The revised contract must distinguish zero tested/model actions from a frozen nonzero setup scan and restore sequence, with setup calls individually verified and separately counted.
- These are boundary facts, not yet a repair decision. Product source remains untouched, and all ALFWorld/THOR symbols remain `UNVERIFIED` pending successful per-instance real runtime gates.

### 2026-07-15 Final-Draft Consistency Findings

- A scan plan containing only deduplicated `OraclePose` values loses the exact-ID/source provenance needed to recompute per-entry `source_rank`. The frozen unit must be a structured request carrying the pose plus all `(exact_id, cache|geometry)` provenances, and the plan hash must cover both. The reset event/initial pose is a zero-action incidental observation or a uniquely initial-visible target can be missed.
- `addressable exact ID`, `unobserved`, `coverage_miss`, and post-reset `absent` need mutually exclusive definitions. Closed-container descendants and inventory objects are ineligible for geometry generation and remain unobserved even if scan frames incidentally reveal them; only an eligible ID missed by a clean complete scan is a coverage miss; a successor ID created after reset starts absent.
- Gate B cannot call `robot_go_to` directly for an initially invisible positive target under the current strict-visibility authorization rule. Each such target needs a frozen public exploration prefix containing no expert/internal ID or setup-scan leak; if no public path can make it strict-visible, Gate A must reject the architecture.
- Typed reset needs ready/terminal exclusivity, nullable early-failure fingerprints, complete setup failure codes, best-effort restore after mid-setup failure, and uncertainty precedence that quarantines/closes the environment. Trace and Outcome must exist before reset so transport spies can prove zero provider factory/send/API calls for ordinary Episodes and tasksets.
- Slice exact-ID behavior is an external runtime linchpin. Until Gate A proves whether SliceObject preserves the ID or creates successor IDs, context rebase is `UNVERIFIED`: preserve-ID permits rebase; replacement consumes/invalidates the old context, does not auto-select a successor, and lets a newly visible successor follow the ordinary absent/unique-parent rule.
- Identity evidence must include actual import origins and source hashes, Unity build/scene asset identity, scan-policy/geometry hashes, cache bytes/hash and reachable raw hash. The only permitted scene normalization is the exact version-scoped `FloorPlanN -> FloorPlanN_physics` mapping; expected manifest/trial mismatch and runtime wrong-scene need separate typed setup classifications.
- Deployment configuration must inject ALFWorld root/config/backend/evidence root without hard-coded host paths or localhost. Evidence references must be relative or opaque and contract-tested with two root/host configurations.
- No-leak Gate B must inspect the whole provider-bound prompt/session/tool schema/result/image-path surface. Setup frames, hidden labels/counts/parents, scene/trial fingerprints, scan plan/snapshot/hash/count and absolute paths are forbidden; model frame zero must come from the verified restore event and use a non-leaking name.
- Result accounting must make successful setup `1 query + N unique scan Teleports + 1 restore`, count actual sends on failure, store setup once at taskset root, keep subtasks model-only, and make `advance_goal()` setup zero. Snapshot identity binds only the stable scene reset; goal context/trace binds the current goal trial.
- Current `AlfworldEnvAdapter.advance_goal()` calls external `thor_env.set_task(...)`, increments goal generation, then catches goal-read exceptions and substitutes `won=false/rate=0`. The revised design needs a separate control-action count and typed terminal result; a failed/missing post-set_task goal read cannot be returned as a ready subtask.
- A public-discovery witness is valid only when the public receptacle's published selected pose is the revealing pose; an arbitrary scan step or target-derived geometry step is a false witness. Removing target-derived provenance must leave the selected step present because of a non-target public receptacle seed, and fresh reset replay must re-prove visibility.
- Closed-container and reset-invisible addressable targets require separate Gate cases. Closed descendants must remain `unobserved` and use a public container/Open witness before unique-parent navigation; direct-required addressable targets need a direct `ok` row plus a public-receptacle witness. The MVP cannot claim arbitrary nested-container chains while receptacle parent fallback is forbidden.
- Reachable evidence needs orthogonal hashes: raw event evidence, raw actionReturn/reachable payload bytes for parser audit, and canonical finite sorted reachable-set bytes for plan identity. Volatile raw event fields or payload order cannot silently change the deterministic plan hash.
- Long-horizon results need root-only control terminal ownership, a separate benchmark control-action count, before/after live scene-only digests, and explicit not-run reasons. Snapshot hash equality alone cannot prove `set_task` preserved the live physical scene.
- Portable root-relative IDs must reject absolute paths, traversal, NUL/ambiguous separators and symlink escape; fingerprints hash normalized POSIX-relative IDs plus file bytes, never the injected absolute root/host.
- Final-byte consistency audit passed exact spec SHA-256 `24c8abd5e0db8e80ea926a7f70aec93c090c8334e1c8ea6458ab1aa59cf811e8` after a complete 1,555-line reread. It found no residual cross-section blocker, counted 70 balanced fences, and explicitly left all external scan/restore/Toggle/Slice/product compositions `UNVERIFIED`.
- Requested post-design full pytest run reached `351 passed, 1 skipped, 1 failed`; the sole failure is the pre-existing cleanup guard. Its global blocked-text list includes the generic word `deterministic`, and canonical baseline `22cb122` already contains that word in the unchanged V1.7 spec and `tests/homemaster/benchmarking/test_alfworld_execution.py`, so the clean baseline is already guaranteed to fail this guard. The revised docs add further legitimate uses but did not create the first failing condition; no product fix was authorized or attempted.
- Excluding only that proven baseline guard, the remaining full suite passed `351 passed, 1 skipped in 11.00s`; focused ALFWorld/Runner/Dispatcher passed `145 in 3.97s`. Ruff lint reports 39 pre-existing issues and Ruff format reports 41 unchanged source/test files; remote git status proves no `src/` or `tests/` file differs from `22cb122`. Compileall passes. These static FAILs remain visible and were not auto-fixed during a design-only task.
- The old untracked V1.8 implementation plan is not executable against the final design: it makes `controller.receptacles` the product store, uses one trial fingerprint for snapshot identity, has no reset scan transaction/provenance/addressability/public witness/goal-control ledger, and schedules plan/code reviews that the user has now explicitly forbidden. It must be replaced wholesale before product code changes.
- Current production layout has no scan/snapshot/reset module: candidate/context logic lives in `execution.py`, while `env_adapter.py` exceeds 3,000 lines and owns reset, goal advance, navigation, manipulation and low-level THOR calls. The replacement plan should add focused `pose_snapshot.py` and `reset_transaction.py` boundaries, retain action context/gateway in `execution.py`, and keep Adapter as runtime binding rather than expanding it further.
- Existing ignored Gate A assets are reusable only as orchestration/evidence scaffolding: `run_gate_a.py`, `oracle_runtime_feasibility_probe.py`, `verify_gate_a.py`, `matrix-v1.json` and README exist remotely. `discovery-run-001` contains only the first failed discovery trial and no exact-case manifest/action run; it must remain immutable root-cause evidence while the replacement Gate uses new empty output directories.

### 2026-07-15 Gate A V2 Actual-Entrypoint Recovery

- The prior verifier mutation suite exercised independent pure v2 derivations but did not prove the production `verify_case_bundle()` called them. Its GREEN result therefore coexisted with a real run path that trusted only three worker-reported counts.
- The probe had the same split: discovery executed the reset snapshot transaction, but `case_main()` still performed one ordinary reset and read `runtime.oracle_controller` through `oracle_lookup_twice`; the run CLI did not even receive `matrix-v2.json`. Syncing those bytes would have produced a predictable real-run failure or a count-only false positive.
- The repaired contract makes each case independently repeat reset/query/all scan Teleports/restore, binds tested initial state to the restore event/frame, and derives the one action pose from the fresh snapshot. Negative lookup statuses remain distinct synthetic mutations and never authorize map access.
- The run verifier now reloads all per-case raw setup artifacts, independently derives addressability/snapshot/restore/witness state, compares the fresh snapshot to the frozen discovery binding, and checks per-case plus summary counts. Slice is derived from before/after exact-ID sets and must resolve one consistent runtime branch.
- Local evidence remains internal: AST entrypoint audit, three self-test suites, compile/Ruff, and a valid/tampered case-artifact pair pass. All ALFWorld/THOR symbols remain `UNVERIFIED` until the same bytes pass on `hkust4` and real Gate A external terminal states pass per case.

## 2026-07-15 Gate A Run-003 Public-Witness Linchpin

- `discovery-run-002` exposed a v1/v2 schema adapter omission after a complete external transaction. RED-first repair now rejects legacy `discovery_contract` authority in all three helpers and preserves completed transaction refs/counts when downstream discovery raises.
- The orthogonal real rerun proves that repair took effect: controller setup count changed from the false `0` in run-002 to the actual `27` in run-003, while query, 25 scan Teleports, restore, raw events, snapshot and cleanup remained independently inspectable.
- The next failure is not an exception or external return-code failure. One exact FloorLamp is revealed only by its own task-independent geometry step. Removing target-derived provenance removes the step, and no public receptacle is visible there; every other scan event leaves the target non-strict-visible.
- This is the specification's explicit authorization failure, not a local implementation defect. Allowing direct hidden fixture labels, public waypoint navigation, or target-derived hidden pose use changes the approved public/no-leak boundary and requires user approval before code changes.
- User selected containment-based authorization: an addressable public semantic target may use its sole reset-time pose while currently offscreen. The obsolete public-receptacle witness is removed as a structural upstream decision rather than broadened into more discovery modes.
- Closed-container descendants are the sole navigation-discovery exception. `robot_go_to("mug")` returns non-terminal `target_not_visible`, performs zero backend action, reveals no parent/container hint, and never navigates to or opens a container automatically.
- The model remains responsible for container exploration. If it never exposes the target, existing no-progress/tool-iteration limits eventually produce Agent failure. After a container is opened, parent resolution is allowed only when the exact target becomes strict-visible in the current event.
- Local current spec is the authoritative newer design draft. The `hkust4` worktree still has an older 1,161-line dirty spec against commit `22cb122`; every unique remote hunk must be shown present or intentionally superseded before synchronization.
- Spec self-review found a state-transition contradiction: generic navigation selected only reset-time `addressable=true` IDs, so a Mug whose frozen row is `closed_ancestor/unobserved` could never become selectable after the model opened its container, despite §7.4 promising that path.
- The consistent resolver must prioritize reset-addressable matches, then allow a stable current-view fallback only when there is no reset-addressable match and an exact matching target is strict-visible with no current closed ancestor. Explicit ordinals remain bound to the stable full matching set and never fall back to another instance.
- Gate A/B currently prove the initial closed-child zero-action failure and ordinary container Open separately, but do not prove the full transition from hidden failure through model-selected Open to post-open child navigation. The written spec must add this black-box sequence before approval.
- Every substantive older remote dirty hunk is present in the local current spec, usually in expanded form: typed TextWorld feedback, no default factory, gateway-only navigation backend, RuntimeTermination mapping, retry causes, taskset not-run accounting, ToolDispatchObserver, split Gate A controller/verifier, import blocker, Gate B identity preflight, candidate-complete trial set and fixed-trial Runner manifest. The remote status line is intentionally superseded by the newer containment decision state.
- Current source verifies the natural Agent-failure path: repeated error tool results increment `AgentState.consecutive_tool_errors`; the default guard is 5 and returns the real code `max_consecutive_tool_errors`, already mapped by the spec to `agent_model_failure`. Repeated identical mixed exploration can instead reach `max_no_progress_iterations=20`, while ALFWorld config caps total tool iterations at 1000.
- `hkust4` real config confirms the same values: `max_consecutive_tool_errors=5`, `max_no_progress_iterations=20`, and ALFWorld taskset `max_tool_iterations=1000`.
- Written-spec self-review passes: 1,591 lines, 70 balanced fences, no placeholder/credential-pattern hits, and clean `git diff --check`. Frozen candidate SHA-256 is `eaa9c97f82aee45c08a4e388189f68ef56cdea73d65e9d34e88e011c781c4ba3`.
- Synchronized `CHANGELOG.md`, spec and three live-state docs to `hkust4`. All five remote hashes exactly equal local committed bytes, remote `git diff --check` exits 0, remote HEAD remains `22cb122`, and remote status contains no product/test/config/helper change.

## 2026-07-15 Visible-Only Navigation Correction

- The user learned a new navigation-module constraint before implementation: a physical target that is not present in the current camera view cannot be navigated to. The just-committed containment-based screen-off direct authorization is therefore superseded, not partially patched.
- The upstream simplification should remove frozen-addressable/current-visible fallback modes from action-time authorization. The reset snapshot may still supply one pose after an exact target is strict-visible, but snapshot presence can never authorize navigation by itself.
- One scope ambiguity remains material: whether the visibility precondition applies to every physical target, including Drawer/Cabinet/Shelf receptacles, or only movable/fixture targets. The spec must not be rewritten until this is explicit.
- In either scope, a current-view miss returns non-terminal `target_not_visible`, sends zero backend navigation actions, preserves context and leaks no hidden parent/pose. Search and memory remain outside this Harness version.
- User confirmed the strict rule applies uniformly to every physical target: receptacles/furniture and movable/fixture targets have the same current-event `metadata.visible=true + positive bbox` authorization gate.
- This upstream rule eliminates containment-based authorization modes. Containment remains relevant only to manipulation preconditions/terminal-state evidence and, after the target is already strict-visible, deterministic unique-parent pose resolution.
- User approved the recommended architecture: retain bounded reset scan and one immutable pose snapshot, but make `VisibleObjectView` the mandatory first authorization boundary. Snapshot lookup is unreachable until an exact target is current strict-visible.
- The first-half rewrite map spans status/decision flow, goals/non-goals, option trade-offs, snapshot language, `VisibleObjectView`, target grounding, resolver ordering and executor preconditions. Reset addressability remains a setup pose-generation predicate only and must never authorize an action-time call.
- Deterministic grounding under visible-only semantics is: generic label selects the first current strict-visible matching exact ID in frozen canonical order; explicit ordinal binds the frozen full matching order, succeeds only when that exact ID is current strict-visible, and returns the same `target_not_visible` for missing/invisible without fallback.
- The second-half rewrite map spans navigation steps/result tables, hidden/offscreen examples, Gate A discovery/case semantics, Gate B no-leak assertions, paired visible/invisible cases, implementation order, invariants and historical disposition. All screen-off direct-positive success language must be removed from current requirements.
- The decisive black-box proof is a per-target paired gate with the same valid snapshot row: current event invisible means `target_not_visible`, zero backend action and no snapshot lookup; a fixture/event where the exact same target is strict-visible permits exactly one locked pose request and full return/terminal-state validation.
- `discovery-run-003` no longer proves that visibility authorization is too strict. It proves only that reset setup found a valid FloorLamp pose while the restored current event did not show the target; under the corrected contract that first product call must be a negative visible-precondition case.
- First reverse scan passes whitespace/fence/placeholder gates and finds no live screen-off success branch. Remaining stale `direct-positive` wording is limited to `required_coverage_set` naming and must become pose-required coverage; §16.3 matches are explicitly historical/superseded.
- The final spec also needs an ordering guard, not only output assertions: production call graph/runtime spies must prove `VisibleObjectView` authorization dominates and an invisible call cannot reach `OraclePoseStore`, parent resolution, context creation or backend send.
- Fresh component reread found the observability schema itself still ordered `oracle_pose_lookup_started` before `oracle_visibility_gate_result`, which would legitimize a hidden lookup despite correct final payload. Visibility gate start/result must precede every pose lookup event.
- Gate visibility fixture data is test-only hidden authority and must be added to the full Provider-bound no-leak denylist; allowing the fixture pose/expected target into prompt/session would make the paired black-box self-fulfilling.
- Gate/history reread found three final consistency edits: explicitly mark §16.3 prose non-normative, include visibility fixture in the GateCase isolation invariant, and require a new one-time plan review after `writing-plans` because the existing untracked plan predates the visible-only architecture.
- Visible authorization must bind external state to what the model actually saw. Added event/model frame hashes and equality to `ObjectObservationRead`; an unseen intermediate event or mismatched image is stale/uncertain and cannot authorize navigation.
- The Gate visible fixture is accepted only after its exact frame becomes the latest model-visible test observation and matches Adapter current event plus persisted/provider image pixels. This prevents a hidden test mutation from manufacturing a false current-view PASS.
- Final visible-only spec self-review passes at 1,614 lines and SHA-256 `b1ee5ef4b402ab55bed793941c86c1b5f3fd900035db8142499b1f5b848c5fac`: 70 balanced fences, clean `git diff --check`, no placeholders/credential patterns, no live stale screen-off/direct-positive semantics, and historical §16.3 explicitly non-normative.
- The five-file visible-only design delivery is synchronized byte-for-byte to `hkust4`. Remote HEAD remains `22cb122`, remote `git diff --check` exits 0, and remote status contains no product, test, config or Gate-helper change; only the known documentation/live-state files plus the separately preserved `CLAUDE.md`, `docs/pitfalls.md` and stale untracked implementation plan are present.
- The user explicitly approved these frozen bytes as the implementation baseline. The older untracked implementation plan predates the visible-only authorization order and is non-executable; it requires wholesale replacement plus one fresh read-only plan review before code changes.

## 2026-07-15 Visible-Only Implementation-Plan Recovery

- The three current Gate A helpers are substantial already-wired v2 programs (probe 5,022 lines, controller 2,820, verifier 7,482) at hashes `1b1ca9e...`, `58b0b43...`, and `7cb32fd...`. Their actual handlers consume fresh reset/query/full-scan/restore artifacts, but discovery schema, case generation, verifier and mutation fixtures still require `public_witnesses`; that authority must be removed from every real consumer rather than bypassed in one self-test.
- Preserve `matrix-v2.json` and runs 001-003 as immutable evidence. The visible-only delta needs a new v3 matrix/case manifest plus `discovery-run-004` and `case-run-004`, with a bijection audit proving the twenty locked trial rows did not drift.
- Run-003's persisted command proves the canonical runtime roots are `/data1/haodong2/weilin/red_bird/{Homemaster,alfworld}` and Python is `/data0/yuqiao/envs/hm_alfworld/bin/python`. `readlink -f` proves the `/home/haodong2/...` paths resolve to those same `/data1` directories; product identities still exclude host/root, while Gate commands use the canonical resolved paths.
- Current `AlfworldEnvAdapter` saves a frame path after reset/action, and tools append that image to a `ToolResultMessage`, but no component records which exact Provider request actually contained the image and produced the assistant tool call. Therefore `metadata.visible + bbox` cannot yet prove “model saw this frame.” The plan needs an explicit successful Provider-attempt/model-view commit ledger and Adapter frame ledger; missing/stripped/mismatched image commits must make visibility stale, not authorize navigation.
- Multiple tool calls from one assistant response all originate from the same Provider request. A frame produced by the first call in that batch is not model-visible for the second call; the model-view commit must remain fixed for the whole batch and advance only after a later successful Provider response.
- Keep new pure reset snapshot logic in focused `pose_snapshot.py`, transaction orchestration in `reset_transaction.py`, and repurpose `execution.py` for the one gateway, visible-first navigation, exact context and manipulation transitions. Do not expand the already 3,677-line Adapter with another independent policy layer.
- The self-reviewed replacement implementation plan is frozen for its one permitted plan review at 971 lines and SHA-256 `9af25b8314b696d43220ec8833d223b87410ea5dd079ed43da991bc4e0e66938`. It uses a new immutable v3 Gate matrix/run namespace, separates Provider attempt facts from Runtime commit state, removes image-stripping/hidden SDK retries, and keeps each intermediate product migration runnable before deleting legacy paths.

## 2026-07-15 Visible-Only Implementation-Plan Review Disposition

- The sole read-only reviewer returned `FIX` on candidate SHA-256 `9af25b8314b696d43220ec8833d223b87410ea5dd079ed43da991bc4e0e66938`, changed no file, delegated no work and did not endorse any external ALFWorld/THOR/SDK symbol.
- Task 4 now claims only setup gateway routing while legacy action consumers remain; Task 6 atomically establishes navigation-only gateway routing, and Task 7 removes the remaining direct manipulation sends before enabling the global sole-gateway invariant.
- Slice identity remains `UNVERIFIED` until real Gate A evidence proves exactly one of `preserved` or `replaced_unique`. Gate A then locks one source constant and one implementation branch; the runtime contract may reject drift but cannot select between dual behaviors.
- Ten-Episode delivery now requires process exit 0, an exact ten-row bijection, all per-instance external gates, 100% evaluation/Harness/provider/runtime coverage or availability, zero unclassified failures and `formal_score_available=true` before final review or commit.
- Provider/model-view identity now uses ordered opaque `frame_binding_id` records plus byte and pixel hashes, so duplicate frames and multi-image requests are deterministic without treating a content hash as a unique event identity.
- Task 7 now inventories every `AlfworldStepResult` constructor and includes all six constructors in `test_alfworld_tools.py` in the same required-feedback migration.
- All five findings are accepted in final plan SHA-256 `f9f8f998a779754c538ff9a2e931ec1f463c58e1297d1a84a4489a4928c977ab` (1,014 lines, 56 balanced fences). The plan review gate is closed; no second plan review is allowed.

## 2026-07-15 Gate A V3 Verifier Pose-Schema Root Cause

- A Gate visibility fixture stores normalized poses as exactly `x`, `y`, `z`, `rotation`, and `horizon`. It does not store a backend action request.
- `_validate_v3_visibility_fixture()` used `validate_requested_pose_schema()` for both `fixture.pose` and `fixture.expected_actual_pose`; that validator correctly requires the additional request-only fields `action=TeleportFull` and `rotateOnTeleport=false`.
- The valid shared manifest therefore failed before any intended v3 mutation with `expected_actual_pose.rotateOnTeleport: required key is missing`. Adding request fields to the manifest would have hidden the verifier defect and mixed two schema layers.
- The targeted repair uses `validate_actual_pose_schema()` for both normalized fixture poses. The unchanged complete fixture now passes, while the existing closed five-field validator still rejects missing/extra pose schema through `require_keys()` and numeric checks.

## 2026-07-15 Gate A V3 Helper Completion Boundary

- The generic grounding fixture freezes canonical exact-ID order and proves that a generic label selects the first current strict-visible peer; an explicit ordinal stays bound to the frozen full matching order and never falls back when its bound exact ID is invisible or missing.
- The post-Open fixture freezes the public exploration/Open call sequence before containment classification. Child identity, snapshot/pose hash and parent metadata mutations cannot alter those public calls; an occluded child performs zero lookup/parent/navigation, while a visible child may use only its unique-parent snapshot pose.
- The same-batch fixture proves the model-view commit does not advance between two tool calls from one assistant response: the first navigation executes once, and the second returns `execution_state_uncertain` before lookup, parent, context or backend access.
- Dead v2 self-test and public-witness derivation functions were removed from executable helper paths. A final symbol/entrypoint audit is still required after remote formatting.
- These are internal contract checks only. The full v3 case bundle, real THOR return codes, Unity build identity, per-case terminal state and independent formal run report remain unverified until fresh `hkust4` runs pass.
- First post-format dead-authority scan finds no executable `verify_public_witness`, `oracle_lookup_twice`, `snapshot_lookup_twice`, `self-test-v2`, or `self_test_v2` definition. Remaining public-witness strings are schema-rejection tests and AST reachability guards.
- Several active helper functions retain `v2` in their names because the raw setup artifact schema was extended rather than wholesale renamed. The verifier also adapts the v3 matrix into the older common trial-row validator internally. These names are not sufficient evidence of a bypass; audit their actual callers and data sources before deciding whether any removal is warranted.
- Controller call-flow proof found a genuine dead legacy branch: `validate_discovery_result()` returns immediately to `validate_v3_discovery_result()` when `schema_version == SCHEMA_VERSION`, then the following condition rejects every remaining schema. Its old `initial_state`/`oracle_entries`/`movable_profiles` body can never execute.
- The dead controller body is the sole caller of `validate_initial_state_raw_event()`, `validate_oracle_entries()`, and `validate_movable_profiles()`. `validate_oracle_entries()` still reads the forbidden matrix `discovery_contract`, so leaving it in place preserves a misleading obsolete authority even though runtime dispatch cannot reach it.
- In contrast, verifier functions with `v2` names are actively called by the current v3 branch and reload raw policy/plan/snapshot/setup artifacts. They are historical names, not dead authorities; do not delete them during the scoped cleanup.
- The RED cleanup guard failed exactly on the three orphan definitions and turned GREEN after their removal; controller self-test count increased from 32 to 33.
- `validate_discovery_result()` still receives `matrix_sha256`, but the active v3 branch did not use it; only the removed dead branch compared `result.matrix_sha256`. This may be a real identity-binding hole rather than harmless signature residue and must be checked against the frozen plan and probe result schema before repair.
- The plan, probe result writer and independent verifier all bind `matrix_sha256`, confirming the controller omission was a real identity hole. A stubbed v3 validator proved a wrong hash was accepted before any artifact check; the controller now rejects it at dispatch and the mutation is permanently named in self-test-v3.
- Task 1 Step 4 requires a complete valid fixture to traverse the actual `verify_case_bundle()` path with zero failures, plus independent snapshot, model-view, fixture-event and action-count tampering. The current verifier self-test creates a full manifest and checks AST wiring but never instantiates `CaseBundle`; this acceptance item is still open.
- A minimal complete invisible bundle is sufficient for this internal orthogonal gate: reset/restored event keeps the target invisible, one frozen scan pose observes it and publishes a valid snapshot row, then case execution returns `target_not_visible` before lookup with zero tested action. This exercises real setup/snapshot/fixture/model-view verification without inventing a synthetic navigation terminal state; real visible navigation remains an external Gate A obligation.
- The verifier independently enforces the intended synthetic design: scan step 0 is the only no-send observation; step 1 must carry provenance and raw successful Teleport evidence; snapshot pose is selected from independently recomputed strict-visible bbox area; oracle frames are raw/PNG decoded and byte-compared; restored state and model-view are independently bound to the invisible tested event.
- The completed synthetic gate proves those validators are actually composed in `verify_case_bundle()`: a valid full bundle has zero failures, while internally rehashed mutations hit the intended independent phases (`snapshot_derivation`, `model_view`, `visibility_fixture`, `setup_count`). This closes Task 1 Step 4's internal fixture requirement, but remains synthetic rather than THOR evidence.
- Formal `hkust4` preflight proves the four `helper-archive/pre-discovery-run-004` hashes match the locked plan and `discovery-run-004`, `case-run-004`, and `exact-cases-v3.json` are absent. The current formal helper files are different intermediate bytes and can be replaced only after preserving the archive.
- Gate A README remains a stale v2 operational document: it instructs `self-test-v2`, matrix/exact-cases/run-002 and public-witness artifacts. Rewrite it to the actual v3 CLI and current-visible contract before formal synchronization.
- Ruff configuration discovery is cwd-sensitive. The first isolated `/tmp` gates used defaults, not HomeMaster's `pyproject.toml`, so their PASS did not prove project lint/format. A non-fail-fast SSH sequence then returned the last self-test's exit 0 despite earlier Ruff failures. Both mechanisms are now explicit release-blocking rules.

## 2026-07-15 Gate A Run-004 Missing-Ordinal Controller RCA

- The first real v3 worker completed in 48.9 seconds with process exit 0, cleanup complete, discovery status PASS, 27 setup actions, 23 frozen cases and zero tested/model actions. Scene identity is the verified `FloorPlan219 -> FloorPlan219_physics` mapping.
- Controller alone rejected case `adb3e7cb...` because its requested ID has no visibility oracle. It is the one intentional `grounding_ordinal_missing` sentinel for public label `FloorLamp 3`; the real frozen type set contains exactly two FloorLamps.
- The sentinel must be absent from policy/snapshot/oracle by construction. Its pair, snapshot-not-applicable and freshness-not-applicable hashes are derived from `(trial_id, missing_exact_id, object_type, ordinal_index)`, matching the independent verifier's existing special branch.
- All six real task-relevant exact IDs have visibility oracles, and no other case lacks one. This isolates the fault to controller manifest validation rather than setup, visibility discovery, THOR return, cleanup or case generation.
- Preserve `discovery-run-004` unchanged and use a new run number after repair; do not regenerate evidence into the failed directory.
- The targeted repair has a named actual-entrypoint order gate plus behavioral checks for valid absence, hash drift and illegal real-oracle presence. Local controller compile/self-test pass; the fixed validator still needs offline replay against immutable run-004 bytes before a new THOR run.
- Offline replay is now orthogonally GREEN: the fixed controller consumed the unchanged real run-004 worker result and every referenced policy/reachable/plan/state/snapshot/oracle artifact, accepted all 23 cases, and preserved the observed 27/0/0 counts. This closes the diagnosed controller gap without claiming a new external run has passed.

## 2026-07-15 Gate A Run-005 Direct-Anchor Ordering RCA

- Trial 1 proves the run-004 controller repair works on fresh external data. Trial 2 completed 24 setup actions with clean process cleanup and zero tested/model actions, then worker status failed on one classification issue.
- The affected Statue is strict-visible, has an `ok` direct geometry snapshot and a paired visibility oracle whose execution anchor is the Statue itself. Its parent Dresser lacks a snapshot row, which is irrelevant after the higher-priority direct pose is selected.
- `classify_semantic_movables()` appended the missing parent-anchor issue before applying the direct target override. Persisted surface/inventory classifications already carried the correct Statue anchor, proving the issue was stale intermediate state rather than missing feasibility.
- Fixed-probe replay on immutable run-005 restored metadata, snapshot and visibility fixtures produces 31 cases with no issues and the same direct Statue anchor. No external rerun has passed yet.

## 2026-07-15 Pre-Run-006 Discovery Decision Audit

- The main-thread audit initially suspected a second direct-anchor ordering hole when an `ok` direct target also has ambiguous innermost parents. Full data-flow tracing rejected that hypothesis: the parent evidence owns the movable location profile as well as the navigation anchor. A direct pose answers where to move but cannot decide whether the object is on a surface or in an open/closed container, so preserving the ambiguity failure is correct and no code change is justified.
- The post-Open case generator freezes `expected_child_strict_visible_after_open` as `container_id == actual_parent_id`. That is an unsupported external-state assumption: opening the actual parent does not prove the child is in the returned camera view. The approved contract requires deriving authorization from the Open return event's exact `metadata.visible=true` plus positive bbox; a still-occluded child must return non-terminal `target_not_visible` with zero child lookup/navigation.
- The case worker currently rejects a real Open return whenever its observed child visibility differs from that guessed boolean, and its expected action/lookup/context counts are also precomputed from the guess. Controller and independent verifier both encode the same assumption in case kinds, counts and run checks, so this is a cross-helper contract defect rather than a probe-only conditional; `discovery-run-006` remains unspent.
- The first offline replay did not reach that repaired contract. The formal worker transaction retains rich in-memory `restored_event` and `restored_capture` objects, while persisted `restored-state.json` is only a normalized state projection. A faithful audit harness must reload the raw event and reconstruct the complete capture envelope, including raw RGB/PNG references and round-trip flag; passing only the normalized state would weaken fixture binding and is not equivalent.
- Immutable run-004 candidate-1 has 23 valid cases but no post-Open kind, and immutable run-005 candidate-2's persisted movable profiles contain only one eligible surface and one eligible inventory object. These two handoff-selected replays are useful regression inputs but cannot by themselves exercise the repaired post-Open branch; locate an additional preserved nonempty worker or keep the repair externally unproven until run-006.
- A bounded scan of every preserved discovery worker (runs 001-005, seven results total) found zero eligible `closed_container` profile and zero post-Open case. Therefore no historical artifact can nontrivially replay the repaired branch. Record old-run replay as compatibility evidence only; run-006 remains the first possible external proof and its independent run verifier must observe both selected outcome envelopes (`ok` and `target_not_visible`).
- The repaired compatibility harness independently reloaded and hash-checked raw events, raw RGB, PNG pixels, normalized restored state, snapshot and visibility oracles. Current probe derivation produced 23/16 cases with empty issues for immutable run-004 candidate-1/run-005 candidate-2; current controller accepted both and `verify_post_open_discovery_bindings()` reported no failure. This proves no regression on those two real artifact graphs, while the explicit aggregate `post_open_case_count=0` prevents claiming branch coverage.
- Preliminary whole-helper reference scan finds no executable stale pre-frozen child-visibility field or old post-Open case kind. The remaining `container_id == actual_parent_id` comparison only derives the frozen container relation (`unique_parent` versus `other`); it no longer selects the child visibility outcome. Preserve that reset-containment relation as independent evidence while auditing every consumer.
- Execution/verifier composition correctly derives child visibility only from the persisted Open return event and chooses one of two closed outcome envelopes; the final verifier requires at least one independently passing `ok` and one `target_not_visible`. One remaining audit question is whether the controller/process-return gate also treats the selected dynamic outcome as authoritative rather than comparing against the case's static top-level status/exit fields.
- The dynamic-status concern is closed without a code change. `target_not_visible` is intentionally not one of the four transport/lookup `NEGATIVE_EXIT_CODES`; an allowed post-Open hidden outcome therefore produces Gate `status=pass` and process exit 0 while retaining `observed_status=target_not_visible` and non-terminal public failure bytes. Controller and independent verifier consume those two layers separately, and the latter independently re-derives the observed layer from the raw Open event.
- Final static composition audit found no remaining stale post-Open authority. Project-config remote gates pass after mechanical formatting: probe 24/24, controller 41/41, verifier 35 mutation/bundle/wiring checks; formatted local and isolated-remote hashes match at probe `2927df15...`, controller `7c958821...`, verifier `0b1bcb94...`.
- Formal pre-run-006 deletion/archive gate passes on `hkust4`: HEAD is `22cb122`, formal helper/README bytes exactly equal `helper-archive/pre-discovery-run-006`, and `discovery-run-006`, `case-run-006`, and `exact-cases-v3.json` are absent. The known dirty Git paths are design/live documents only; product/config/test files remain frozen.
- Final formal preflight after synchronization exits 0. Exact formal hashes are probe `2927df1503f9e6ab08d561aff4b39799131528ef13e8a5298e541b142a84462b`, controller `7c9588211f9e30d822bed69a264f5a0a471c01924faa91b015194170b17e1618`, verifier `0b1bcb946e2d826ca396304918338a11a809914d4b41fc6a8fb8fea8bdbaff78`, README `2e94c9ca...`, matrix-v3 `5a0c7816...`. Compile, project lint/format, 24/41/35 self-tests and final-byte immutable compatibility replay all pass in the formal repository.
- Fresh `discovery-run-006` passed all three episode-0001 candidates, including both prior failure points, then stopped on `episode-0002-historical-exact`. Its worker completed 28 setup actions, clean cleanup, zero tested/model actions, 18 cases and two eligible closed-container profiles, but retained `closed-child transition lacks its target-independent parent sequence`; no manifest was created. The fault is discovery composition after successful external setup, not an engine return failure.
- Run-006 trial 4 proves a schema impossibility rather than unlucky candidate choice. Both hidden CreditCards have unique reciprocal parent `Drawer|-05.02|+00.88|+00.37`; that Drawer is addressable with snapshot status `ok` and a sole pose. Both child visibility oracles correctly remain `unobserved`, `visible_fixture=null`, `execution_anchor_exact_id=null`, because production oracle construction only resolves a parent when a current visible fixture exists. `_post_open_cases()` nevertheless requires each closed child's visibility oracle to predeclare that parent and its snapshot hash, so every real closed child is rejected before container iteration.
- The locked plan explicitly says closed descendants remain unobserved and gain a positive transition only through the separate public Open sequence. The architecture-consistent repair is therefore to derive/freeze post-Open child execution binding separately from raw reset reciprocal containment plus the parent snapshot row. Enriching the visibility oracle would conflate authorization with a future pose; opening containers during discovery would violate the frozen setup transaction; dropping the Gate would lose required behavior.
- The scoped implementation removes the impossible dependency from five real consumers: probe discovery and fresh-case revalidation, controller discovery binding, verifier fresh-case setup and verifier discovery binding. Child oracle hashes still bind the child identity/freshness; raw reset containment independently binds the exact parent; the parent snapshot independently binds the sole execution pose. Local compile and self-tests are GREEN at probe 26, controller 42 and verifier 36 checks.
- Immutable trial-4 replay after that repair still produced no post-Open rows because its Drawer had no visibility oracle at all. `_build_visibility_case_oracles()` filters only `_required_physical_types(trial)`; target-independent public containers outside task object/target types are omitted. The synthetic GREEN fixture manually supplied container oracles and therefore hid this second production-shape gap.
- A raw audit of all nine frozen public closed containers in trial 4 proves the existing bounded scan is sufficient: every row has snapshot status `ok`, at least one strict-visible event and 25-28 invisible events across 29 persisted events. No scan/action expansion is needed. The correct deterministic oracle coverage set is the union of task-required physical exact IDs and exact container IDs from the already frozen target-independent public container order.
- Implemented that union at the producer/consumer boundary without changing authorization semantics. Probe freezes the closed-container order from the reset event and emits rows for `(trial physical types) union (closed public container exact IDs)`; controller independently derives container IDs from the persisted initial raw event; verifier independently derives the full container order from the same raw event at both discovery and fresh-case entrypoints.
- Local compile and second-layer behavior/wiring gates are GREEN at probe 27/27, controller 43/43 and verifier 37 total mutation/bundle/wiring checks. These remain internal evidence; immutable run-006 must rebuild the oracle artifact from raw setup data before the repair is externally grounded.
- Project-config isolated `hkust4` gates pass after mechanical probe formatting: compile, Ruff lint/format, probe 27/27, controller 43/43 and the complete verifier suite. Final candidate hashes are probe `a120d634c6172905896d8c0a0f3fe76b6b334f873dcba3f43b772ff102a97e70`, controller `7bf0db62176610f316455826e3a5e661e4d2804c35845f0857cbf8157c6ea1ac`, verifier `cefe847a524ac7a4befcc71e69d605251c9ca5878fc2d3a80dae8baadfd7a22a`.
- The rebuilt-artifact replay is orthogonally GREEN on immutable run-006 trial 4. It reconstructed and hash/pixel-checked reset, all 26 scan steps and restore; rebuilt visibility oracles from 6 to 15 rows; covered all nine public closed containers; derived 46 cases including 9 post-Open cases (1 unique-parent, 8 other-container); retained zero issues; and passed both the controller actual validator and verifier's complete discovery artifact path.
- `pre-discovery-run-007` locally and remotely matches the final probe/controller/verifier/matrix/README bytes exactly. README preserves `discovery-run-006/` only as immutable failure evidence and routes every executable command to fresh run-007/case-run-007 paths.
- Formal `hkust4` preflight exits 0 from a fail-fast shell: HEAD and dirty whitelist, run deletion guards, exact hashes/archive equality, run-006 identity/count invariants, doc hashes, compile, project Ruff lint/format, 27/43/verifier self-tests, and final-formal-helper rebuilt replay all pass. Immutable `discovery-run-006/summary.json` SHA-256 is `36bcfe4baef404df4f60452a67091111ea4513d21d1d385846ada64688c63b34`.
- Fresh run-007 proves the closed-container union repair in the real producer: trial 4 now passes with 28 setup actions, 15 oracle rows, 46 cases, zero issues and zero tested/model actions. Trial 5 then fails independently after four setup actions; run-007 summary SHA-256 is `9633c3d94c16f5345288f53f022457e6c73fd960fcc3ffb9fdac9f8c3fe07de2`, and no manifest/case directory exists.
- Trial-5 raw projection diff isolates exactly three fields: two Eggs and one Lettuce change `ObjectTemperature` from `Cold` to `RoomTemp` after the third successful Teleport. Exact IDs, transforms, reciprocal containment, all boolean action states, inventory, `heated_objects`, `cooled_objects`, `cleaned_objects` and goal remain unchanged. ALFWorld 0.5.0 source confirms heat/cool goal scoring uses its explicit runtime sets, not raw THOR `ObjectTemperature`.
- `PausePhysicsAutoSim` and `UnpausePhysicsAutoSim` both return success in ai2thor 2.1.0 but do not stop temperature evolution, so that candidate is rejected. `ChangeTimeScale(0.01)` and restoration to `1.0` both return success; a full query + 26-pose scan + exact restore remains byte-semantically world-stable, and after restoring 1.0 the same three objects resume warming on post-control Teleport 7. This proves both the slowed and restored external behaviors on the real build.
- The user selected the fixed time-scale bracket. The formal spec now preserves raw `ObjectTemperature`, freezes `0.01 -> 1.0` in the scan policy, changes successful setup accounting to `N+4`, restores pose then normal time on every post-enter failure, makes the successful normal-time return event the sole published/model-visible event, and reserves fresh run-008 namespaces while preserving runs 006/007.
- A return code alone cannot prove that simulation time resumed. The written Gate therefore adds a build-scoped orthogonal effect-control worker: every frozen temperature sentinel must remain stable through the maximal slow scan and must independently resume evolution after `1.0`; this evidence is separate from, and cannot replace, each worker's own per-request return/world/final-event checks.
- The 1,652-line spec self-audit passes: SHA-256 `92ef48a68a30f021aa81cadf5177e07ecf9d12ecda924744a50a4c6f612a9f49`, 70 balanced fences, no placeholder, no stale successful `N+2` contract, no executable run-007 output path, no credential pattern and clean `git diff --check`.
- Design commit `61b76dc` contains only the spec and matching CHANGELOG entry. The spec/CHANGELOG/task plan/findings/progress hashes are byte-identical on hkust4; immutable run-006/run-007 summary hashes remain `36bcfe4b...`/`9633c3d9...`, and both discovery-run-008 and case-run-008 remain absent.
- The user explicitly approved implementation. The existing implementation plan is updated in place rather than reviewed again: 1,207 lines, 70 balanced fences, SHA-256 `ed91439f0ae0f6453084c37f95032600467e8a4e5d225f1c60c99877e916bd71`, with exact matrix fields, five setup phases, failure terminal/recovery ownership, per-sentinel effect control and run-008 commands.
- Plan self-review caught and fixed two false-green risks before code: the slow-request-attempt flag is set before the external send so exceptions cannot skip normal-time recovery, and immutable run-007 must remain a temperature-drift failure under the new verifier rather than being reinterpreted by injected schema fields.

## 2026-07-17 Gate Override And Product Start

- Gate A recovery advanced through immutable discovery runs 008-015. Run 015 executed all 20 workers: 19 passed, while `supplemental-slice-contract` failed because an unrelated Apple settled after the first slow-time request. No exact-case manifest was created, and the failure remains visible.
- Continuing to model every engine-settling edge inside the standalone Gate helpers would duplicate product behavior and increase the verifier surface. The user explicitly directed the implementation to stop pursuing perfect Gate A, begin product edits, and run Gate B as non-blocking evidence that exposes failures.
- The unfinished controlled-settling helper draft was archived under `helper-archive/abandoned-controlled-settling-draft-after-run-015`; active helper bytes were restored exactly from `pre-discovery-run-015`.
- Product implementation now proceeds RED-first. Gate A/B failures remain `UNVERIFIED`; neither may be aggregated away or described as PASS.

## 2026-07-18 Product Implementation And Gate B Findings

- Every ordinary taskset trial is now built and verified before Adapter construction, and trial data loads only through `load_verified_trial_data()`. Typed reset/goal terminal results stop before Provider construction and assign setup/control/model counts to the root owner.
- Provider retry authority is no longer split: LLMClient sends exactly one frozen request with one selected key; GenericRuntime owns at most one retry for three exact error/cause pairs. Attempt IDs, request hashes and frame bindings are call-scoped, and a successful model view commits before tool dispatch.
- Formal V1.8 navigation/manipulation entrypoints use current-visible authorization, a sole snapshot pose, one gateway and typed feedback. AST/runtime guards show the V1.8 call graph cannot reach V1.7 candidate navigation/local-Put paths, but those compatibility implementations remain physically present; strict zero-legacy-symbol source cleanup is not achieved.
- Real Gate B found a unit-test coverage hole: monkeypatched `build_alfworld_batch_env_with_first_trial` replacements accepted arbitrary positional arguments and hid a keyword-only production signature mismatch. The regression now uses a strict keyword-only replacement.
- Direct complete-diff review found that disk trial identity was verified but the actual THOR runtime scene was not compared with the selected logical scene. The Adapter now accepts only the exact version-scoped `FloorPlanN_physics -> FloorPlanN` mapping; a wrong physics scene or bare logical scene terminates with zero setup actions and closes the environment.
- After that repair, the historical FloorPlan219 slice reached THOR but one planned scan Teleport did not round-trip to the exact `OraclePose`. Recovery attempted pose restore and normal-time restore, but the final event did not satisfy the full initial pose/world/visibility/frame equality. The environment closed and no snapshot or partial model run was published.
- Fresh `run-003`, executed after the runtime-scene fix, proved that valid FloorPlan219 passes the new identity gate and then reproduced the same reset terminal and counts as `run-002`. It is the final production-affecting Gate B rerun; neither run is a PASS.
- Gate B correctly delayed Provider construction: the reset terminal produced zero Provider requests/attempts and zero model backend actions. This proves the lifecycle stop, but it means the product Provider/image/Dispatcher/tool chain was not externally exercised by `run-002` or `run-003`.
- The independent Gate B verifier's `slice_verified=true` is artifact consistency only. With zero Provider rows its request/image loop is vacuous, and its mandatory `overall_status=incomplete` plus exit 2 remain the authoritative aggregate. Documentation must never call this a behavioral slice PASS.
- The reset result advertises `evidence_ref=reset-transaction.json`, but neither `run-002` nor `run-003` contains that persisted file. The typed summary and trace preserve the terminal fields, yet the named raw reset ledger is missing; this is a known evidence-persistence defect.
- Gate A's absent exact-case manifest still blocks the complete 20-case Gate B action matrix, but it does not block product trial selection. Frozen `matrix-v3` already separates six `historical_exact` rows from four unresolved three-candidate groups; selecting each group's pre-Gate `candidate-1` is deterministic, independent of Gate outcomes and explicitly labeled `deterministic_replacement` rather than historical recovery.
- The earlier API check inspected environment variables only and missed ignored `config/homemaster.yaml`. The existing Mimo profile has one configured key and passes a project `LLMClient` request through `anthropic_sdk`; `mimo-v2.5` returned visible text `OK`. No credential was printed, rewritten or added to Git.
- The fixed run `alfworld-valid_unseen-v18-realapi-20260718-001` validated all ten manifest entries and reset all ten exact trials. Every row ended `execution_state_uncertain` at trigger `scan_pose_mismatch`, final `scan_time_scale_restore_rejected`, with five setup requests and no Provider/model/tool request. Aggregate evidence is 50 setup requests, 0/10 raw success, 0% evaluation/Harness coverage and no formal score.
- CLI reports `provider_availability=1.0` for this run because no Provider failure occurred. Since setup stopped before Provider construction, the authoritative Provider execution count is zero; availability must not be presented as proof of a model request.
- The final direct review found no additional blocking defect after the runtime-scene repair. With the committed-manifest regression, the plan's exact focused command closes at `193 passed` and the repository-wide run at `395 passed, 1 skipped`; static/structured guards pass. The inherited format baseline is recorded rather than bulk-rewritten.
